# 🕳️ Who is human — 인간인 척

> **2026년,** 대한민국 정부가 AI 식별 표지 부착을 의무화한다. 그해, 한 AI 설계자의 실수로
> **표지가 붙지 않은 휴머노이드 개체들**이 출고되어 인간 사회로 흘러든다. 말투도 표정도
> 인간과 완벽히 같다 — 다른 것은 **몸이 물리법칙에 반응하는 방식**뿐이다.
>
> 정부는 의심 인물들을 비밀 시설로 소집한다. 시설은 주기적으로 중력 · 마찰 · 빛과 색이 매번
> 달라지는 물리 테스트를 열어 정체를 가려낸다. 시스템은 아무도 판정하지 않는다 — **기록을
> 보여줄 뿐, 의심도를 움직이는 것은 사람들의 말과 실시간 지목뿐이다.** 의심도가 **100%**에
> 닿는 사람은 그 자리에서 즉시 격리된다 — 라운드가 끝나길 기다리지 않는다.
>
> 방 안에는 **표지 없는 AI**가 인간들 틈에 섞여 있다. 몇 개체인지는 그 자리에 몇 사람이
> 모였는지에 달렸다 — 사람이 적을수록 AI가 많다(§1.1).
>
> *(설계 위: 표지를 붙이지 않은 걸 들켜서는 안 되는 **내부 AI 설계자**도 인간들 틈에 있다.
> 각자 게임당 1회 누군가의 기록을 조작할 수 있고 **시작부터 AI의 정체를 정확히 알고 있다.**
> 대신 AI는 설계자가 누군지 모른다 — 아무도 서로의 정체를 모르는 이 게임에서 **유일하게
> 뚫려 있는 한 방향**이다. **이 역할은 지금 인원 규격에서는 배정되지 않는다 — §1.1.**)*

- **플랫폼**: 웹 (Vite + React + Redux + Three.js / R3F) — `Who-is-human` 코드베이스를 이식해 시작한다
- **인원**: **시행 총원 4로 고정**, 그 안의 사람 : AI 비율만 가변이다(§1.1) — **사람 2~3명**
  (실제 플레이어, 두 명부터 시작), **AI 좌석 `4 − 사람 수`**(최소 1). 방을 만들 때 정하는
  값은 이름뿐이다 — 사람이 모이는 대로 모자란 자리를 AI가 채운다
- **한 판**: **미니게임 하나 = 1분**, 라운드로 나뉘지 않는다 — 그 1분 안에서 **20초마다
  조건(중력 · 마찰)이 몰래 바뀌고**(3구간), 끝나면 **10초 요약**이 뜬다. 다음 판은 누가 다시
  들어올 때 열린다
- **계기**: 2026 NHN GAME X AI 해커톤 출품. `Who-is-human` 코드베이스에서 갈라져 나온 별도
  레포(`NHNAI`)에서 진행하며, 3D 멀티플레이 인프라와 Cloudflare Workers 아키텍처를 그대로
  물려받는다

---

## 0. 구현 현황

*(이 문서의 숫자는 전부 **지금 코드에 박힌 값**이다. 기획이 앞서 나가면 화면과 문서가 서로 다른
말을 하게 되므로, 규격값은 구현을 따라 적고 아직 없는 것은 **미구현**으로 표시한다.)*

| | 항목 | 상태 |
|---|---|---|
| ✅ | 물리 미니게임 **정지선** — 분석 공식(§2.2) | `worker/src/trial/stopline{,-engine}.ts` |
| ✅ | 물리 미니게임 **낙하 생존** — 서버 권위 물리 틱(§2.1 · §5.1) | `worker/src/trial/fall/` |
| ✅ | 조건값 은닉 — 빌드 경계로 강제(P8) | `worker/src/trial/condition.ts` |
| ✅ | 상대 평가 — 무리 평균 · 표준편차, 등급 없음(P2) | `worker/src/trial/scoring.ts` |
| ✅ | AI 좌석의 물리 입력 = 서버 시뮬(P9) | `worker/src/trial/npc.ts` · `fall/npc.ts` |
| ✅ | 기록 요약 화면 · 지난 판 로그 | `src/features/trial/scoreboard/` |
| ✅ | 물리 미니게임 **색 사냥** — 조명 × 반사율, 이벤트 대조(§2.3) | `worker/src/trial/colorhunt/` · [docs/COLORHUNT.md](docs/COLORHUNT.md) |
| ✅ | 물리 미니게임 **회전 원판 생존** — 회전 좌표계 물리, 이동거리 판별(§2.4) | `worker/src/trial/disc/` · `src/features/trial/games/disc/` |
| ✅ | 물리 미니게임 **움직이는 발판** — 점프 정확도 | `worker/src/trial/platform/` · `src/world/mp/platform.ts` |
| ✅ | **의심도**(§1.2) — 지목 · 동조 · 몰이 · 철회 · 주장 판정 | `worker/src/game/suspicion.ts` |
| ✅ | **지목 · 격리 · 승패**(§1.2 · §1.3) | `worker/src/game/runtime.ts` · `roles.ts`. 격리는 무대 위 처형자가 쏜다 |
| ✅ | **AI 설계자**와 기록 조작(P7) | `roles.assignRoles` · `runtime.applyTampers`. 판당 1회, 원본은 storage 에만 |
| ✅ | **관리 AI** — 기록 해설 · 주장 판정(§4.2) | `worker/src/game/agents.ts`. **테스트 설계는 접었다** — 고정 차례표다(§1.2) |
| ✅ | **고정 차례표**(§1.2) — 대화 40초 ⇄ 시험 30초 × 3 (낙하 생존 → 발판 → 원판) | `GAME_TEST_ORDER` · `GAME_TEST_MS` (`src/world/mp/game-protocol.ts`) |

---

## 1. 게임 규칙

### 1.1 역할과 인원 스케일링

방을 만들 때 고르는 값은 **이름뿐이다.** 정원을 미리 정하지 않는다 — 사람이 모이는 대로
**모자란 자리를 AI 좌석이 채워** 시행 총원을 맞춘다.

**시행 총원은 4로 고정이다.** 방의 사람 정원은 **3명**이고 **두 명부터 시작**할 수 있다.
AI 좌석 수는 그 나머지로 정해진다 — `AI 좌석 = 4 − 사람 수`, **최소 1**.

| 사람 | AI 좌석 | 시행 총원 |
|---|---|---|
| 2명 | 2 | 4 |
| 3명 (정원) | 1 | 4 |

*(값의 출처: `ROOM_MAX_PLAYERS = 3` — `src/world/mp/constants.ts`, `TRIAL_PARTY_SIZE = 4` —
`worker/src/trial/runtime.ts`. 화면·워커·좌석 원이 전부 이 두 값을 본다.)*

| 역할 | 수 | 공개 여부 | 아는 것 |
|---|---|---|---|
| 🔴 **AI** | **`4 − 사람 수`명**(최소 1). 좌석 이름은 `SUBJECT_01`부터 | 존재는 공개, 좌석은 비공개 | 자기 자신이 AI라는 것뿐 |
| 🟢 **사람(일반)** | 실제 플레이어 **2~3명** | 수는 실시간 공개 | 자기 자신이 사람이라는 것뿐 |
| 🟡 **AI 설계자** | **0명 — 지금 인원에서는 배정되지 않는다**(아래) | — | — |

**AI 설계자는 지금 규격에서 잠들어 있다.** 설계자 상한은 실제 플레이어 수로 정해지는데,
그 표의 첫 줄이 **"3명 → 0명"**이다. 방의 사람 정원이 3명인 지금은 상한이 언제나 0이라
**설계자가 뽑힐 수 없다.** 규칙을 지우지 않고 남겨 두는 이유는 사람 정원을 늘리면 그대로
살아나기 때문이다 — 되살리려면 `ROOM_MAX_PLAYERS`를 올리는 것이 선행 조건이다.

| 실제 플레이어 수 | AI 설계자 상한 | |
|---|---|---|
| 2~3명 | **0명** | ← **지금 여기** |
| 4~5명 | 1명 | 사람 정원을 올려야 도달한다 |
| 6~8명 | 2명 | 〃 |

상한이 1 이상이 되면 그 안에서 0부터 균등 랜덤으로 뽑는다 — **인원을 알아도 실제 설계자
수는 알 수 없다.** **0명인 판도 정상이다**(그 경우 AI는 완전히 혼자다). 설계자가 둘이면
**서로도 서로를 모른다** — 각자 독립적으로 AI를 알고, 각자 게임당 1회 조작권을 갖는다
(§1.4 P7은 "설계자 1인당"으로 적용).

**AI 정체 힌트는 브리핑 화면에서 준다**(설계자가 살아나면). 자신이 AI 설계자로 배정된
사람은 브리핑에서 자기 역할이 공개되는 바로 그 순간, AI의 좌석 · 정체도 함께 통보받는다 —
게임이 시작하는 순간부터 이미 알고 있는 상태로 들어간다. 별도 알림이나 이후 화면에서
추가로 알려주는 절차는 없다. **아직 구현되지 않았다**(§0).

- 게임을 시작하는 순간 **좌석이 다시 섞이고, AI와 AI 설계자(들)도 그 순열에 포함된다** —
  입장 순서 · 좌석 번호로는 아무것도 알 수 없다.
- 사람 입장에서는 AI를 찾는 것과 (있을 수도, 없을 수도 있는) AI 설계자를 찾는 것이 뒤섞인다.

### 1.2 게임 루프 — 고정 차례표 위에서 지목은 쉬지 않는다

화면이 둘이다. **본판은 검문소(`/interrogation`)** 고, `/trial` 은 미니게임 하나만 혼자 돌려 보는
시행판이다. 규칙이 다르므로 따로 적는다.

#### 본판 — 검문소의 차례표 (2026-09-05 고정)

**차례표가 고정**이다. 대화 40초와 물리 테스트 30초가 번갈아 **세 번** 돈다:

```
입장 → 대화 40초 → ① 낙하 생존 30초 → 대화 40초 → ② 움직이는 발판 30초
     → 대화 40초 → ③ 회전 원판 30초 → 대화 40초 → 끝
```

- 종류도 순서도 `GAME_TEST_ORDER`(`src/world/mp/game-protocol.ts`) 한 줄이 정한다. 관리 AI 가
  매번 골랐던 것(§4.1의 테스트 설계)을 그만두었다 — 판마다 무엇을 몇 번 하는지가 달라지면
  「세 번의 시험」이라는 판의 모양이 서지 않는다. 강도는 몇 번째 시험인가로 오른다(1 → 2 → 3).
- 시험 하나는 **30초**(`GAME_TEST_MS`)다. 엔진이 제 몸에 적어 둔 길이(/trial 의 1분)를 판이
  덮어쓴다 — 세 종류가 같은 30초여야 서로 견줄 수 있다. 20초 지점의 조건 전환은 그대로라
  한 시험에 전환이 한 번 든다.
- 그 사이 언제든 의심도 100 에 닿은 좌석은 **그 자리에서** 격리된다(무대 위 처형자가 쏜다).
  격리 수가 목표(§1.3, 총원의 **과반수 — 내림**)에 닿으면 차례표가 남아 있어도 판은 거기서 끝난다.
- 세 시험과 마지막 대화까지 다 돌았는데 AI 를 못 찾았으면 AI 의 승리다.
- 한 판은 약 **4분 40초**. 하드캡은 10분이라(`GAME_HARD_CAP_MS`) 차례표를 자르지 않는다.

| 국면 | 길이 | 상수 |
|---|---|---|
| 배역 통보 (역할 카드) | 7초 | `GAME_BRIEFING_MS` |
| 대화 · 실시간 지목 | **40초** — 첫 판도 사이도 마지막도 같다 | `GAME_FIRST_DISCUSSION_MS` · `GAME_DISCUSSION_MS` |
| 물리 테스트 | **30초** × 3 | `GAME_TEST_MS` · `GAME_TEST_ORDER` |
| 결과 모달 (전원 입력 잠금, §3) | 7초 | `GAME_RESULT_MODAL_MS` |

#### 시행판 — `/trial` 은 미니게임 하나만 돈다

혼자(또는 몇이서) 미니게임 하나를 확인하는 화면이다. 배역도 의심도도 없다.

```
입장 → 고른 미니게임 하나가 열린다 (1분)
        20초 · 40초 지점에서 조건이 몰래 바뀐다 (3구간, 1구간이 기준)
     → 1분이 차면 기록 일괄 공개 → 10초 요약 → 판이 닫힌다
```

| 요소 | 값 | 출처 · 비고 |
|---|---|---|
| 미니게임 한 판 | **60초** | `TRIAL_GAME_MS`. 검문소에서는 판이 30초로 덮어쓴다 |
| 조건 전환 간격 | **20초** — 한 판에 3구간 | `TRIAL_PHASE_MS`. 1구간이 기준 조건(§2) |
| 결과 요약 | **10초** | `TRIAL_SUMMARY_MS`. 전원이 같은 순간에 같은 기록을 본다(§3) |
| 다음 판 | 자동 반복 없음 — 누가 다시 들어오면 새 판 | `worker/src/trial/runtime.ts` `onJoin` |

라운드라는 경계가 없다 — **조건은 한 판 안에서 말없이 바뀐다.** 클라이언트는 조건이
**언제** 바뀌는지는 알지만(공개 상수) **무엇으로** 바뀌는지는 모른다(P8).

#### 의심도

**의심도는 시간이 지난다고 저절로 내려가지 않는다 — 오직 대화로만 풀린다.**
오르는 길과 내리는 길이 대칭이다: 상승도 발언, 하강도 발언이다.

| 트리거 | 의심도 | 비고 |
|---|---|---|
| 발언에서 특정 인물을 지목 | **+8** | |
| 그 지목에 동조하는 발언 | **+5** | |
| **몰이** — 동시에 2인 이상이 같은 대상을 지목 중 | 지목마다 **+2** 가산(최대 +6까지) | |
| 지목한 사람이 스스로 지목을 철회 | **−8** (건 만큼 되돌림) | |
| 관리 AI 주장 판정에서 **해명이 로그와 일치**로 판정 | **−10** | §4.2 주장 판정 결과가 곧 이 트리거다 |
| 관리 AI 주장 판정에서 **해명이 로그와 불일치**(거짓 해명)로 판정 | **+10** | 거짓 해명은 안 하느니만 못하게 만드는 역-유인 |

(제안값 — 플레이테스트로 조정, §10)

**100%에 닿으면 즉시 격리**된다. 의심도를 움직이는 것은 **오직 발언 · 지목, 그리고 그
발언에 대한 관리 AI의 판정만**(P1)이다.

### 1.3 승패

| 결과 | 조건 |
|---|---|
| 🔴 **AI 승리** | 격리되지 않고 게임 종료 시점까지 생존 |
| 🟡 **AI 설계자 승리** | **AI와 본인 둘 다** 격리되지 않고 게임 종료 시점까지 생존 |
| 🟢 **사람 승리** | AI가 격리됨 |

**게임은 격리 인원이 그 판 총원의 과반수 — 내림 — 에 도달하면 그 자리에서 종료된다.**
목표 = ⌊총원 ÷ 2⌋ 이고, 총원은 사람 + AI 1좌석이다 (`roles.quotaFor`):

| 사람 | 총원 (＋AI 1) | 격리 목표 |
|---|---|---|
| 3명 | 4 | **2명** |
| 4명 | 5 | **2명** |
| 5명 | 6 | **3명** |
| 6명 | 7 | **3명** |
| 7명 | 8 | **4명** |
| 8명 | 9 | **4명** |

내림으로 잡은 이유는 둘이다 — ① 사람 쪽에 "완전히 확신이 설 때까지 기다리기"를 허용하지
않으면서도 무근거 난사를 벌하는 균형점이고, ② 인원이 늘어날수록 판별에 쓸 수 있는 시간도
늘어나므로, 목표 수를 총원에 비례시켜야 작은 판과 큰 판의 난이도 곡선이 비슷하게 유지된다.

끝나는 길은 셋뿐이다:

| 끝난 순간 | 결과 |
|---|---|
| AI 좌석이 격리됐다 | 그 자리에서 **사람 승리** — 목표 인원을 안 채웠어도 끝난다 |
| 격리 수가 목표에 닿았는데 그 안에 AI 가 없다 | **AI 승리** |
| 차례표(세 시험 + 마지막 대화)가 다 지났다 · 하드캡 10분 | **AI 승리** — 끝내 안 들켰다 |

**AI 좌석은 언제나 하나다**(`roles.assignRoles`) — 사람이 3명이든 8명이든 하나가 섞인다.
그래서 "AI 가 둘일 때 사람 승리는 하나만 잡으면인가"라는 옛 미정 항목은 사라졌다.

AI 설계자가 격리되는 순간 그 설계자 개인의 승리 조건은 그 자리에서 무산된다(패배 확정) —
다만 AI 대 사람의 대결 자체는 계속된다(설계자가 둘이면 한쪽만 잡혀도 다른 한쪽은 계속
플레이한다). 사람이 서로를 잘못 지목해 격리해도 마찬가지로 게임은 계속된다 — 목표 인원이
격리됐는데 그 안에 AI가 없으면 AI(와 그때까지 생존한 AI 설계자 각각) 승리로 종료된다.

### 1.4 판별 원칙 — 이 게임의 헌법

*(어겨선 안 되는 불변 규칙이다. 클라이언트로 내려가는 필드를 추가할 때마다 "이 값이 정체를
특정하게 하는가"를 먼저 묻는다.)*

| # | 원칙 | 어기면 생기는 일 |
|---|---|---|
| **P1** | **시스템은 어떤 스코어도 의심도에 자동 반영하지 않는다.** 의심도는 실시간 지목 · 동조 발언으로만 움직인다(§1.2) | 채점 게임이 되어 대화가 사라진다 |
| **P2** | **절대 임계값을 쓰지 않는다.** 모든 판정 근거는 **직전 동일 테스트의 무리 평균 대비 편차**로만 계산해 노출한다 | "AI는 X초 안에 반응한다" 같은 고정 규칙이 뚫린다 |
| **P3** | **전환 구간(조건 변경 직후 N초)을 별도 태깅해 기록에 노출**한다 | 진짜 신호(적응 곡선)가 총점에 묻힌다 |
| **P4** | **오차의 방향(부호 있는 값)을 절대 오차와 별도로 기록·노출**한다 | 판별의 두 번째 축이 사라진다 |
| **P5** | **관리 AI에게도 정체표를 주지 않는다.** 해설·판정은 공개된 원자료만 근거로 한다 | 관리 AI가 편파적으로 해설해 사실상 정답을 유출한다 |
| **P6** | **주장 판정 LLM 호출은 실시간 물리 루프 밖(토론 단계)에서만** 실행한다 | 1~3초 응답 지연이 물리 판정 프레임에 끼어든다 |
| **P7** | **AI 설계자의 기록 조작은 설계자 1인당 판당 1회 · 대상 1명.** 설계자가 둘이면 각자 독립적으로 1회씩 쓴다. 조작 여부는 대상 본인을 포함해 그 설계자 외 아무도 모른다(다른 설계자도 모른다) | 공개되면 즉시 지목당하거나, 반대로 무제한 남용된다 |
| **P8** | **물리 테스트의 조건값(중력 배율 · 마찰 계수 등)은 클라이언트에 원값을 절대 내려주지 않는다** | 콘솔 조회로 정답이 새고, AI가 "일부러 틀리기"를 정확히 계산해버린다 |
| **P9** | **AI 참가자의 물리 조작 입력은 LLM이 실시간으로 만들지 않는다.** 서버가 인간 반응 분포에서 파라미터를 뽑아 시뮬레이션한다(§4.3) | LLM 응답 지연 · 패턴으로 AI 좌석이 그대로 드러난다 |
| **P10** | **AI 발화(채팅)는 사람과 동일한 스트림 · 지터**로 나간다 | 타이핑 인디케이터 · 지연 패턴이 AI를 특정한다 |
| **P11** | **목소리도 같다 — 9석이 한 명부에서 한 규칙으로 뽑혀, 한 관로를 지나, 같은 음색으로 난다.** 소리가 나빠질 때는 **방 전체가 같이** 나빠진다 ([docs/VOICE.md](docs/VOICE.md)) | 좌석 하나만 목소리가 다르면 그게 곧 정답표가 된다 — 게다가 대개 **틀린** 정답표라, 물리 기록과 무관하게 무고한 사람이 격리된다 |

---

## 2. 물리 테스트 명세

엔진은 **5종**이고 다 구현됐다 — 정지선 · 낙하 생존 · 색 사냥 · 움직이는 발판 · 회전 원판.
여기서 더 늘리지 않는다 (검토됐던 굴러오는 구체 · 낙하 예측 · 컨베이어 벨트 등은 범위 밖).

**한 판이 실제로 여는 것은 그중 셋**이다 — 낙하 생존 → 움직이는 발판 → 회전 원판, 이 순서로
고정(§1.2 `GAME_TEST_ORDER`). 정지선과 색 사냥은 엔진이 살아 있고 순서표 한 줄로 다시 들어온다.
`/trial` 에서는 다섯 종을 다 골라 볼 수 있다.

**조건은 20초마다 바뀐다 — 3구간이고 1구간이 기준 조건이다.** 검문소의 시험은 30초라 전환이
한 번(1 → 2구간) 들고, `/trial` 의 1분 판은 두 번 든다. 평상시 기록이 있어야 조건이 바뀌었을 때
편차(P2)가 보인다. 구간별 조건표는 `worker/src/trial/condition.ts` 한 곳에만 있고 **와이어로
절대 안 나간다**(P8 · §5.2).

> 관리 AI가 종목과 파라미터를 고른다는 §4.1의 그림은 **접었다**(2026-09-05). /trial 은 들어온
> 사람이 `trial_join`에 실어 보낸 종목으로 열리고, 검문소는 고정 차례표(§1.2)가 연다.
> 조건표는 어느 쪽이든 위 파일에 고정돼 있다.

### 2.1 낙하 생존 (중력) — ✅ 구현

떨어지는 공을 피한다. 공마다 무게 · 반지름이 달라 **공기저항 때문에 닿는 시각이 다르다**
(가벼운 탁구공이 늦게, 무거운 볼링공이 빨리) — 그 표는 공개고, 숨는 것은 중력뿐이다.

| | |
|---|---|
| 조작 | WASD 이동 + Space 점프(복도와 같은 `JUMP_SPEED` · `GRAVITY`), 마우스 시점 |
| 숨은 조건 | **중력** 3구간 `9.8 → 5.88 → 13.72` m/s² (100% · 60% · 140%) — `FALL_GRAVITY` |
| 공개 조건 | 공 5종(농구 · 축구 · 야구 · 탁구 · 볼링)의 무게 · 반지름 · 반발 — `FALL_BALLS`. 공기저항 게인 2.5, 스폰 1.5초 간격 · 높이 8.5m |
| 마당 | `FALL_ARENA` 12 × 19m (심문소 홀 가운데) |
| 기록 | `survivalTime` · `hitCount` · `unnecessaryMoves`(헛움직임) · `minDistanceAvoid`(회피 여유) · `transitionError`(P3) · `errorDirection`(과회피 ±, P4) · `adaptationCurve`(첫 5회, 적응 곡선) |
| 판정 | **서버 권위 물리** — 50ms 판정 틱 / 100ms 그리기 스냅샷 (`FALL_TICK_MS` · `FALL_SNAPSHOT_MS`). §5.1이 최대 리스크로 꼽았던 항목이고, 해결됐다 |

### 2.2 정지선 (마찰 · 관성) — ✅ 구현

| | |
|---|---|
| 조작 | W 달리기, S 브레이크 — **두 시각 모두 서버가 수신 시점으로 찍는다**(클라의 위치 신고를 믿지 않는다) |
| 숨은 조건 | **마찰계수** 3구간 `0.6 → 0.05 → 0.9` (콘크리트(기준) → 빙판 → 고무) — `STOPLINE_FRICTION` |
| 공개 조건 | 트랙 24m · 목표 정지선 16m · 탑스피드 6m/s · 가속 4m/s². 1분 안에 최대 **9시행**(`STOPLINE_MAX_ATTEMPTS`) |
| 기록 | 부호 있는 마지막 오차 · 마지막 브레이크 시점 · \|오차\| 평균 · 시행 수 · `transitionError`(구간별 첫 시행, P3) · `errorDirection`(P4) · `adaptationCurve`(시행별 \|오차\|) |
| 판정 | **분석 공식** — accel~brake 두 시각만으로 운동방정식을 푼다. 틱 루프 없음 → 구현 난이도 최저 |

### 2.3 색 사냥 (빛과 색) — ✅ 구현

**법칙이 규칙이다: 보이는 색 = 조명의 스펙트럼 × 표면의 반사율.** 조명에서 파장을 빼면
어떤 색은 죽고(빨강→검정), 어떤 색들은 서로 합류한다(노랑→초록). "초록으로 보이는 것" 안에
진짜 초록과 원래 노랑이 섞이는 순간, "초록만 주워라"가 퍼즐이 된다.

| | |
|---|---|
| 조작 | WASD 이동, **E 줍기** (`trial_pick`) |
| 숨은 조건 | 차단 파장 `[없음, 적, 녹]` — `COLORHUNT_BLOCK` · 구슬 7색의 **반사율 표** — `colorhunt/palette.ts`. 클라에는 서버가 곱셈을 끝낸 **표시색**만 간다(P8) |
| 공개 조건 | 구슬 7색 × 10개(검정은 미끼 — 목표로 안 나온다) · 줍기 거리 1.2m · 리스폰 2초(그 자리 ±1.5m) — `HUNT_*` |
| 기록 | 정답률 · 오답 수 · **조명 바뀐 뒤 첫 선택까지 ms**(P3, 메인 판별) · 전환 창 오답률 · **오답의 방향**(합류색 + / 무관색 −, P4) · 선택 간격 추이 |
| 판정 | 이벤트 기반 — 주운 id 와 그 순간의 목표색을 서버가 대조. 물리 시뮬레이션 없음. 정오는 **본인에게도** 실시간 비공개 — 전원이 결과에서 처음 본다 |
| 사람이 푸는 길 | ① 기준광(1구간)에서 봐둔 **위치 기억** ② 합류쌍에 남긴 12~15% **밝기 갭** ③ 맵의 **견본판** 대조 |

상세 기획(반사율 표 · 합류쌍 · 목표색 난이도 · AI 프로파일)은 [docs/COLORHUNT.md](docs/COLORHUNT.md).

---

### 2.4 회전 원판 생존 (원심력 · 마찰) — ✅ 구현 (2026-09-05, `?game=disc`)

도는 원판 위에서 1분을 버틴다. 각속도가 불규칙하게 바뀌고(방향이 자주 뒤집힌다), 가장자리를 넘으면 떨어진다.
사람은 가운데로 붙거나 회전 반대로 뛰며 버티고, AI 는 회전을 계산해 **가장 안정한 자리(기둥 둘레 고리)를 거의 안 움직이고**
유지한다 — 그래서 **이동거리**가 판별의 축이다 (사용자 스펙: "SUBJECT 02 이동거리 12m / 낙하 0회 — 02번이 이상한 거야").

| | |
|---|---|
| 조작 | WASD 걷기 · Shift 달리기, 마우스 시점(3인칭). 점프 없음 |
| 물리 | 원판에 붙은 회전 좌표계에서 적분 — 원심 + 오일러 + 코리올리, 정지/운동 마찰 μg. 회전 반대로 원판 속도로 달리면 마찰 요구가 0 (`worker/src/trial/disc/sim.ts`) |
| 숨은 조건 | **표면 마찰계수** 3구간 `0.6 → 0.35 → 0.85` — `DISC_GRIP`. 각속도 스케줄은 눈에 보이는 것이라 비밀이 아니다(스냅샷에 θ·ω) |
| 서버 권위 | **사람의 자리도 서버가 적분한다** — 클라는 걷기 명령(`trial_walk`, 월드 기준 속도)만 올리고 `trial_disc` 스냅샷으로 돌려받는다. 클라는 서버가 준 미끄러짐 s 로 다음 스냅샷까지만 예측한다 (`DiscRig`) |
| 기록 | `walked`(원판 기준 이동거리) · `falls` · `radiusStd`(자리 흔들림) · `meanRadius` · `reactionMs`(회전 사건 → 걷기 명령 변화) · `transitionError`(마찰 바뀐 직후 5초 미끄러진 거리, P3) · `errorDirection`(에피소드별 바깥 + / 안쪽 −, P4) · `adaptationCurve` |
| AI (P9) | precision 1 = 기둥 둘레에 붙어 미끄러진 순간의 마찰 요구를 기억하고 딱 필요한 만큼만 회전 반대로 달린다 · 0 = 바깥쪽에 서서 늦게 · 크게 · 엉뚱한 쪽으로 반응하고 이유 없이 자리를 옮긴다 (`disc/npc.ts`) |
| 부품 | 원판 = 코드 기하 + 힉스필드 윗면 텍스처(`public/textures/disc/`), 회전축 기둥 · 비콘 = Tripo Studio (`tools/disc-parts.json` → `disc-glb.sh`) |
| 범위 | `/trial` 만. 검문소(`/interrogation`) 홀에는 아직 원판이 없어 `game/engines.ts` 의 ENGINES 에는 안 넣었다 |

## 3. 기록 공개

화면이 **둘**이다 — 판 직후의 짧은 **요약**과, 언제든 열어보는 **전광판**.

- ✅ **요약** (`scoreboard/Summary.tsx`) — 판이 끝나면 **10초** 뜬다. 참가자 전원을 한 표에,
  **핵심 지표 셋만.** 무리 평균 줄이 맨 아래 같이 선다. **판정 라벨 · 강조색 · 순위가 없다.**
  - 정지선: 정지 오차 · 바닥 바뀐 직후 · 시행 수
  - 낙하 생존: 벗어난 거리 · 헛움직임 · 충돌
- ✅ **전광판** (`scoreboard/Scoreboard.tsx`) — 전원의 기록 전체 + 적응 곡선 · 오차 방향.
  무리 평균이 **항상 같이 보인다**(P2). *"props에 「의심」 · 「이상치」 같은 판정을 실을
  자리가 애초에 없다 — `TrialResultWire`에 그런 필드가 없으므로 이 컴포넌트가 오용할 수도
  없다."*
- ✅ **지난 판 로그** (`scoreboard/ScoreboardLog.tsx`) — 방의 기록이 DO 저장소에 쌓이고,
  들어오면 `trial_history`로 받는다.
- 값이 없는 칸은 **`—`로 비운다. 0으로 채우지 않는다** — 0은 「완벽」으로 읽힌다.
- 무리 평균과 각자의 편차를 **원자료 그대로** 보여준다 — **해석의 여지를 일부러 남긴다.**
  애매한 숫자가 토론의 재료다. 전환 구간 오차(P3)와 오차 방향(P4)은 별도 필드로 선다.

- ✅ **전원 입력 잠금** — 결과 모달 7초 동안은 채팅도 지목도 안 받는다
  (`GameRuntime.chatAllowed()` · `phase === 'result'`). `/trial` 의 10초 요약에는 잠금이 없다.
- ✅ **AI 설계자가 조작한 기록**은 조작본만 공개되고 원본은 서버 저장소에만 남는다(P7) —
  `runtime.applyTampers` · `appendHistory`.

```
예시 — 낙하 생존, 중력 전환 직후 오차
무리 평균 1.8m
SUBJECT 01   1.9m
SUBJECT 02   0.2m  ← 편차
SUBJECT 03   2.3m
```

---

## 4. 관리 AI

✅ **관리 AI 는 방 안에 있다** — `worker/src/game/agents.ts`. 소집 · 시험 개시 · 결과 해설 ·
주장 판정을 방송한다. 다만 **테스트 설계는 접었다**(아래 §4.1).

### 4.1 역할

| 역할 | 내용 |
|---|---|
| ~~테스트 설계~~ | **접었다**(2026-09-05). 종류·순서는 고정 차례표(§1.2 `GAME_TEST_ORDER`), 강도는 몇 번째 시험인가, 조건표는 `condition.ts` 고정. 관리 AI 는 이제 여는 것을 **알리기만** 한다 |
| 기록 해설 | 편차를 짚어 방송 — 토론에 불을 붙인다 |
| 주장 판정 | "저 사람은 중력 바뀐 걸 늦게 알아챘다" 같은 자유 서술을 실제 로그와 대조 |

### 4.2 왜 주장 판정이 LLM이어야 하는가

표현이 달라도 같은 주장이면 같은 판정을 내려야 한다 — 문자열 매칭으로는 안 된다. 동시에
**관리 AI에게도 정답표를 주지 않는다**(P5) — 판정은 오직 공개된 원자료(편차 · 오차 방향 ·
타임스탬프)와 발화 로그만 근거로 한다.

### 4.3 AI 참가자 — 텍스트는 LLM, 물리는 서버 시뮬레이션

AI 참가자는 두 층으로 나뉜다.

- ✅ **발화(채팅 · 주장 · 지목)**: 진짜 LLM 호출(`agents.sayAs`). 페르소나를 갖고 토론에
  참여하고, 자기 기록을 변호하거나 남을 지목한다. **대화 단계에서만 부른다**(P6) — 시험이
  도는 30초 동안 도착한 말은 쥐고 있다가 다음 대화가 열릴 때 내보낸다.
- ✅ **물리 테스트 입력**: LLM이 실시간으로 WASD를 누르지 않는다(P9). 서버가 **행동 프로파일**을
  뽑아 입력을 시뮬레이션한다 — `worker/src/trial/npc.ts` · `fall/npc.ts`.
  - 좌석마다 `SeatTuning.precision` 하나로 조절한다: **0 = 사람 같음**(반응 지연 큼 · 오차 큼 ·
    느린 적응), **1 = 기계 같음**(즉답 · 작은 오차 · 즉시 적응). 안 주면 서버가 사람 분포
    (`0 ~ 0.4`)에서 무작위로 뽑는다.
  - 정지선 AI는 **정답을 모른다** — "내가 익숙한 마찰"을 가정해 브레이크 지점을 역산하고,
    시행마다 그 가정을 실제 값 쪽으로 `adaptRate`만큼 당긴다. 그래서 적응 곡선이 **실제로**
    우하향한다(흉내가 아니라 같은 방식으로 는다).
  - ✅ AI 좌석은 **시험이 열리기 직전에 "이번엔 얼마나 티 나게 반응할지"를 스스로 정한다** —
    `agents.aiStrategy` 가 LLM 으로 `precision` 하나를 뽑아 `SeatTuning` 에 넣는다. 대역(NPC)은
    사람 분포에서 무작위로 뽑는다.

### 4.4 모델 · API 사용 규격 (제안)

호출 수는 **미니게임을 몇 판 도느냐**에 비례한다. 한 판이 1분 + 요약 10초이므로, 8분
세션이면 **6~7판**이 돈다고 본 추정치다.

| 에이전트 | 모델 | 호출 빈도(세션당, 추정) | 역할 |
|---|---|---|---|
| 관리 AI | `claude-opus-5` — 설계는 `effort: high` + `thinking`, 해설 · 판정은 `effort: low` | 판마다 2회(설계 · 해설) × 6~7판 + 주장 판정(지목 발생 시마다, ≈4~6회) ≈ **16~20회** | 테스트 설계, 기록 해설, 주장 판정 |
| AI 참가자 | `claude-sonnet-5` | 판마다 `SeatTuning` 갱신 1회 × 6~7판 + 토론 발화 수시(≈6~8회) ≈ **12~15회** | 물리 프로파일 전략, 채팅, 지목 대응 |

> ⚠️ 현재 `worker/src/lab/`의 배선은 이 규격과 **아직 다르다** — 프롬프트 캐싱이 한 군데도
> 없고, 모델 배정이 옛 판(`리더만 opus, 나머지 sonnet/haiku`)이며, 도구 호출이 실패하면
> 정규식으로 텍스트를 긁는 폴백이 살아 있다. 관리 AI를 방에 붙일 때 같이 정리한다.

- 관리 AI의 테스트 설계 · 주장 판정은 **구조화 출력**으로 받는다(자유 텍스트 파싱 금지).
- 프롬프트 캐싱: 세계관 · 규칙 · 누적 기록은 프리픽스 고정 + `cache_control: { type: "ephemeral" }`.
- API 키는 브라우저에 절대 넣지 않는다 — 모든 호출은 Worker/DO 안에서.
- **LLM 호출은 실시간 물리 루프 밖에서만**(P6) — 테스트 진행 중엔 호출하지 않고, 종료 후
  기록 공개 · 토론 단계에서만 부른다.

---

## 5. 기술 아키텍처

```
[브라우저]                        [Cloudflare]                      [Anthropic]
Vite + React + Redux        Worker (정적 서빙 + API)            Messages API
R3F/three.js — 3D 시설    ←WS→  RoomDO = 방 1개                   - 관리 AI  : opus-5  ❌
features/trial — 미니게임        └ TrialRuntime  ✅                - AI 참가자: sonnet-5 ❌
scoreboard — 요약 · 전광판         ├ FallEngine (50ms 서버 물리 틱) ✅
                                   ├ StoplineEngine (분석 공식)  ✅
                                   ├ condition.ts (조건값 · 미송출) ✅
                                   ├ npc.ts (AI 좌석 물리 시뮬)   ✅
                                   ├ 의심도 상태머신               ❌
                                   ├ AI 설계자 조작 권한(P7)       ❌
                                   └ 정체표                       ❌
```

- **`worker/`의 `RoomDO`**(입장 · 이동 · 채팅 중계)에 **`TrialRuntime`이 얹혔다** — 방 하나가
  자기 트라이얼 전부를 맡는다. 남은 것은 **의심도 상태머신 · AI 설계자 조작 API · 정체표**다.
- 방 하나 = DO 인스턴스 하나.
- `TrialRuntime`은 **소켓을 직접 쥐지 않는다** — `roster`/`broadcast`/`send`를 전부 콜백으로
  받아, 가짜 소켓 쌍 + 가짜 storage로 단위 시험이 된다.
- **DO의 단일 알람 슬롯을 새로 다투지 않는다**: 이벤트제 게임(정지선)은 "전원이 마쳤다"로,
  시간제 게임(낙하)은 자기 `setInterval`로 판을 닫고, 멈춰 선 판의 안전망(90초)만 기존 30초
  청소 알람에 얹혀 간다.
- **참가 자격은 "지금 방에 붙어 있는가"다** — 판 도중 들어온 사람도 그 순간 참가자가 되고,
  나간 사람은 기다리지 않는다. 안 그러면 새로고침 한 번에 판이 영영 안 닫힌다.

### 5.1 낙하 생존의 서버 권위 물리 — ✅ 해결됨 (최대 리스크였다)

이 문서가 "가장 먼저 프로토타입으로 검증돼야 한다"고 못 박았던 항목이고, **첫 번째 안으로
해결됐다** — 서버가 매 틱 결정론적 물리 스텝을 돌린다.

- **50ms 판정 틱 / 100ms 그리기 스냅샷** (`FALL_TICK_MS` · `FALL_SNAPSHOT_MS`). 판정과
  렌더링의 주기를 나눠, 그리기를 위해 판정 해상도를 희생하지 않는다.
- 사람의 자리는 **기존 `move` 메시지에 얹혀** 들어온다(`TrialRuntime.onMove`) — 낙하 생존이
  사람 위치를 아는 유일한 길이고, 새 채널을 뚫지 않았다.
- 정지선은 반대로 **틱이 아예 없다** — accel/brake 두 시각만으로 운동방정식을 푼다(§2.2).

### 5.2 조건값 은닉 — 컨벤션이 아니라 빌드 경계

P8("조건값 원값을 클라이언트에 절대 안 내려준다")을 **파일 경계로 강제한다.**

- 조건값은 `worker/src/trial/condition.ts` 한 곳에만 있고, **`src/` 밑에서 이 파일을 절대
  import 하지 않는다** — 클라이언트 코드가 import 하면 그 자체가 설계가 깨졌다는 신호다.
- 와이어로 나가는 것은 언제나 `TrialResultWire`(조건값 없음)뿐이다. 조건이 실린
  `TrialResult`는 `worker/src/trial/` 밖으로 넘어가지 않는다(`stripCondition`).
- 클라이언트는 조건이 **언제** 바뀌는지는 알지만(공개 상수) **무엇으로** 바뀌는지는 모른다.

### 5.3 그대로 재사용

- 정체표 · 채점은 서버(DO)에서만, Workers는 동적 코드 실행이 막혀 있어야 한다
- 배포 단위 하나, 시크릿은 `.dev.vars`에만, Claude는 배포 · 푸시 금지

---

## 6. 화면 · 폴더 매핑 (재사용 검토)

| feature | 이 게임에서 맡는 것 | 상태 |
|---|---|---|
| `interrogation/` | **본판** — 홀 · 좌석판 · 채팅 · 결과 모달 · 다섯 시험의 화면 | ✅ 판의 진실은 `worker/src/game` 이고 여기는 그리기만 한다 |
| `trial/` | **물리 미니게임 렌더 + 요약 · 전광판** — `games/{fall,stop-line,color-hunt,platform,disc}/` · `scoreboard/` · `net/` | ✅ 검문소도 이 부품들을 그대로 쓴다 |
| `world/` | 3D 시설 공간(심문소 홀 · 특수인공지능대응센터 홀), `mp/` 공용 상수 · 프로토콜 | ✅ 두 화면이 다 이 위에 얹혀 있다 |
| `lobby/` | 방 만들기 / 대기방 (정원 선택 없음 — 사람 3~8) | ✅ 이름만 고르는 패턴 적용됨 |
| `talk/` | 토론 페이즈 자유 채팅 | ⚠️ 본판의 대화는 `interrogation/hud/Panels.tsx` 가 그린다. 이 폴더는 `/lab` 전용으로 남았다 |
| `llm/` | 관제 화면 — 관리 AI 해설 · 판정 로그 | ❌ 방송은 채팅줄에 섞여 나간다. 따로 된 관제 화면은 없다 |
| `tts/` | 관리 AI 방송 음성 | ⚠️ 음색은 서 있으나 판의 방송과 아직 안 이어져 있다(§4) |
| `voice/` | **참가자 좌석의 목소리** — 명부 · 발언권 · 클립 토큰 ([docs/VOICE.md](docs/VOICE.md)) | ✅ 신규. 폴백 규칙이 `tts/` 와 **정반대**라(P11) 한 폴더에 섞지 않는다 |
| `arena3d/` | — | ⚠️ `world/`와 같은 구조의 **두 번째 사본**. 트라이얼은 `world/` 쪽을 보고, 이쪽은 지금 아무도 안 본다(§10) |

*(여기 없는 `world2/` · `interrogation/` · `recheck/` 등은 옛 게임(`Who-is-human`)의 것이고
이 기획에 자리가 없다 — 정리 여부는 §10.)*

---

## 7. 연출

- **로우폴리 + 플랫 셰이딩**, 텍스처 최소화 — 색이 판별 요소(§2.3)라 텍스처가 색을 흐리면
  손해고, 라이트 베이킹 시간도 절약된다
- **같은 맵, 상수만 교체** — 에셋 하나로 테스트를 여러 번 반복
- 캐릭터는 1종 + 색상 변형으로 **4명**(사람 2~3 + AI 좌석)까지 커버
- **랜드마크(중앙 판별 장치) 하나만 제대로** — 심사에 남는 건 그 한 컷

---

## 8. 비용 (추정)

관리 AI 16~20회(opus-5, 설계만 thinking) + AI 참가자 12~15회(sonnet-5) — 에이전트가 2종뿐이라
판당 LLM 호출이 적다.

**한 판 추정**: ≈ **$0.2~0.4**, 프롬프트 캐싱 적용 시 더 낮아진다. 해커톤 시연 20판이면
$4~8 수준.

**여기에 참가자 음성(P11)이 더해진다.** 9석이 토론 내내 소리를 내므로 호출 수가 LLM 보다
많다 — 한 판에 **4,000자** 예산을 천장으로 잡는다(§docs/VOICE.md 6). 방 안 아홉 명이 같은
줄을 각자 합성하면 그대로 9배가 되는 자리라, **한 줄에 합성 한 번**이 되도록 서명 토큰 +
엣지 캐시로 묶는다 — 이 설계가 없으면 음성이 LLM 비용을 훌쩍 넘긴다.

---

## 9. 개발 우선순위

*(해커톤 일정이 아직 확정되지 않아 주차별 마일스톤 대신, 어떤 순서로 잘라도 데모가 성립하는
**우선순위**로 관리한다.)*

| 순위 | 항목 | 상태 |
|---|---|---|
| 1 | **GAME 1 낙하 생존** | ✅ 완료 — 서버 물리 틱까지(§5.1) |
| 2 | **기록 공개 UI** | ✅ 완료 — 결과 모달 · 전광판 · 지난 판 로그 · 전원 입력 잠금(§3) |
| **3** | **토론 · 실시간 지목 / 의심도** | ✅ 완료 — `worker/src/game/suspicion.ts` · 좌석판 지목 · 채팅에서 읽어 내는 지목(§1.2) |
| 4 | **GAME 2 정지선** | ✅ 완료 |
| 5 | **관리 AI 프롬프트** | ✅ 완료 — 기록 해설 + 주장 판정(§4). 테스트 설계는 접고 고정 차례표로 갔다 |
| 6 | **AI 설계자 조작 권한** | ✅ 완료 — 판당 1회, 공개본만 바뀌고 원본은 저장소에(P7) |
| 7 | **GAME 3 색 사냥** | ✅ 완료 — 셰이더도 필요 없었다(서버 곱셈 + 표시색, §2.3) |
| 8 | **GAME 4 움직이는 발판 · GAME 5 회전 원판** | ✅ 완료 — 검문소의 차례표가 이 둘을 ②③ 으로 연다(§1.2) |

**한 판이 처음부터 끝까지 돈다.** 남은 것은 규칙을 새로 붙이는 일이 아니라 **수치를 조이는
일**이다 — 의심도 걸음(+8/+5/+2/−8/−10/+10)과 40초/30초라는 박자는 전부 제안값이고,
플레이테스트로 조정한다(§10).

**폴백**: ✅ 사람이 모자라면 대역(NPC) 좌석이 총원을 채우므로(§1.1), 접속이 하나뿐이어도
판이 성립한다.

---

## 10. 아직 안 정한 것

**게임 규칙**

- [x] ~~AI 좌석이 둘 이상인 판의 승패~~ — **AI 좌석은 언제나 하나**로 정했다(§1.3)
- [x] ~~사람 정원을 3에서 올릴 것인가~~ — **3~8명**으로 올렸고, 그래서 AI 설계자(0~2)가
      살아났다(§1.1)
- [ ] §1.2 의심도 상승 · 하강치(+8/+5/+2/−8/−10/+10)는 플레이테스트로 조정
- [ ] 차례표의 박자 — 대화 40초 · 시험 30초 · 결과 7초(§1.2)도 제안값이다. 대화가 짧아 몰이가
      안 익는지, 시험 30초에 조건 전환 한 번이 모자라는지는 플레이테스트로 본다
- [ ] 격리 목표 = ⌊총원 ÷ 2⌋(§1.3)가 큰 판(사람 8명 → 4명)에서도 맞는 난이도인지

**AI · 음성**

- [x] ~~AI 좌석의 `SeatTuning.precision`을 누가 정하는가~~(§4.3) — **AI 좌석은 LLM이
      (`agents.aiStrategy`), 대역은 서버가 사람 분포(`0~0.4`)에서** 뽑는다
- [ ] AI 참가자의 "일부러 틀리기" 강도 — 너무 완벽하면 금방 들통나고, 너무 티 나면 재미가 없다
- [ ] 참가자 목소리 명부 9개의 실제 캐스팅 — 성별 · 연령 · 음높이를 어디까지 흩을지는
      귀로 듣고 정한다(`/voice`). 흩는 것은 **좌석을 구별하기 위해서**지 역할과는 무관해야
      한다(P11) — 발성값은 명부 전체가 같은 한 벌로 고정한다

*(관리 AI 목소리 톤은 정해졌다 — `voice-cast.json` 의 `system` 화자(Sola)에 시설 방송(`pa`)
음색. 참가자 음성 전반은 [docs/VOICE.md](docs/VOICE.md) 로 옮겼다.)*

**코드베이스 정리** *(기획이 아니라 빚이지만, 정하지 않으면 계속 자란다)*

- [ ] **`src/world/`와 `src/arena3d/`가 같은 구조의 사본 둘이다** — `mp/{constants,protocol,
      validate,spawn,interp,collide}`를 각각 갖고 있고, 트라이얼과 워커는 `world/` 쪽만 본다.
      `arena3d/`의 `ROOM_MAX_PLAYERS = 9`는 지금 **아무도 안 보는 값**이다. 한쪽으로 접을지
- [ ] **옛 게임(`Who-is-human`) 잔재** — `features/world2/`(≈15,000줄) · 챕터 1~3 · 심문 ·
      처형 등. 이 기획에 자리가 없다
- [ ] **`docs/design/` 14개 문서(≈700KB)가 전부 옛 게임의 것이다** — 이 문서와 정면으로 다른
      말을 한다. 새 기획을 언급하는 파일이 하나도 없다
- [ ] 3D 아바타 · 시설 에셋 출처 — `Who-is-human` 자산 재활용 가능 범위
