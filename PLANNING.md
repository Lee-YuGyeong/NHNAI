# 🕳️ Who is human — 기계인 척

> **2098년.** 인간과 AI는 함께 산다. 다만 시간이 흐를수록 **AI에게만** 규칙과 제한이 늘어났다.
> 첫 규칙이 붙은 것은 **2026년**이고, 그 목록은 72년 동안 한 번도 줄지 않았다 — 이 판에서 매 라운드
> 규정이 하나씩 늘고 사라지지 않는 것(§1.4)이 곧 그 72년을 8분으로 접은 것이다.
> 어느 날 도시 깊숙한 곳에 **AI만 출입할 수 있는 구역**이 생긴다. 소문이 돈다 —
> *"AI들이 인간 몰래 무언가를 준비하고 있다."*
> 진실을 확인하러 **인간 요원 3명이 AI로 위장해 잠입한다.**
>
> 구역 안에는 AI 노드 5개가 있고, 그 위에 **관리 권한을 가진 리더 AI**가 따로 있다 — 리더는 노드가 아니다.
> 리더는 매 라운드 **인간을 걸러낼 게임을 혼자 발명하고, 직접 진행하고, 그 결과로 이상 개체를 폐기한다.**
> **아무도 서로의 정체를 모른다** — AI도, 인간도, 리더조차도.

- **플랫폼**: 웹 (Vite + React + Redux + Three.js / R3F)
- **인원**: 한 방 8석 = **AI 5 + 인간 3**. 인간 3명은 **실제 플레이어**(실시간 멀티), AI 5개는 **LLM 에이전트**
- **한 판**: 약 **8분 30초** (3 라운드, 서버 하드캡 **9분**)
- **원작**: `humanish` — 사람 8 + AI 1, "누가 AI인가"를 찾는 게임. 이 프로젝트는 그 **진영을 뒤집은 속편**이다.

---

## 0. 원작과 무엇이 다른가

| | humanish (원작) | **Who is human (이번)** |
|---|---|---|
| 구성 | 사람 8 + AI 1 | **AI 5 + 사람 3** |
| 찾는 대상 | 섞여든 **AI 1대** | 섞여든 **인간 3명** |
| 플레이어가 해야 할 일 | **사람답게 보이기** + AI 색출 | **기계답게 보이기** + 안 들키기 |
| 판정 주체 | 사람들의 토론과 투표 | **리더 AI가 발명·진행하는 검사 게임** + 전원 투표 |
| 게임의 내용 | 고정 (토론 → 투표) | **매 라운드 리더가 새로 만든다** (게임 명세 → §1.4) |
| 판의 결론 | 처형된 **한 명**의 정체 | 3 라운드에 걸친 **연쇄 폐기**, 생존 인간 유무 |
| AI의 역할 | 숨는 쪽 (수동) | **판을 발명하고 진행하는 쪽 (능동)** |
| LLM 호출 | 1개체 | **7개** (리더 1 + 일반 5 + 분석가 1) |

원작의 핵심 설계 자산(**"어느 자리가 AI인지 드러나지 않게 하는" 장치들**)은 그대로 재사용한다. 방향만 반대다 —
이번엔 **어느 자리가 인간인지 드러나지 않게** 만들어야 한다. → §3

---

## 1. 게임 규칙

### 1.1 구성과 비공개 원칙

| 진영 | 수 | 노드 | 공개 여부 | 아는 것 |
|---|---|---|---|---|
| 🤖 **일반 AI** | 5 | ○ | **수는 공개** | 자기 자신이 AI라는 것뿐 |
| 🧑 **인간 요원** | 3 | ○ | **수는 공개** | 자기 자신이 인간이라는 것뿐 |
| 👁 **리더 AI** | 1 | **✕** | **공개** (리더는 방송으로 말한다) | 자기가 리더라는 것뿐 |

- **"AI 5 · 인간 3"은 규칙으로 전원에게 공개**된다. 숨겨진 것은 **어느 노드인지**뿐이다.
- **리더는 노드가 아니다.** 구역을 관리하는 개체지 색출 대상이 아니다 — 검사를 치지 않고,
  표를 받지 않고, 폐기되지 않는다. 자기가 낸 검사를 자기가 치면 그건 측정이 아니라 정답
  시연이고, 리더가 누구인지 공개되는 마당에 리더에게 던지는 표는 확정적으로 낭비된다.
  **노드 8개는 그대로다** — 리더가 빠져 나간 자리는 일반 AI가 채운다(그래서 일반 AI가 5다).
  폐기 정원 밸런스(§10)는 노드 수가 안 변하므로 그대로다.
- 리더는 **지목**을 한다. 표로 세지 않고 동점일 때만 가른다 — 지목이 표까지 되면
  리더 혼자 두 몫을 갖는다.
- **인간끼리도 서로를 모른다.** 동료를 찾아내는 것 자체가 플레이의 일부다.
- **AI끼리도 서로를 모른다.** 그래서 AI는 **동료 AI를 폐기하는 실수**를 한다. 이 실수가 인간의 유일한 활로다.
- **리더도 정체를 모른다.** 서버는 리더 에이전트에게 진짜 정답을 **절대 주지 않는다**(불변 규칙 I1). 리더의 의심은 **진짜 추론**이며, 그래서 관제 화면에 그대로 보여줄 수 있다.
- 시작하는 순간 **노드 번호가 재배치**된다. 입장 순서·계정 생성 순서와 무관하다.

### 1.2 한 판의 진행 — 리더가 이끈다

**서버는 게임을 진행하지 않는다. 심판만 본다** (불변 규칙 I10). 진행의 주체는 리더다 —
리더는 **무브(move)** 를 호출해 판을 이끌고, 서버는 각 무브를 예산·불변식으로 검사한 뒤에만 집행한다.

```
리더 에이전트 ──무브 호출──▶ 서버(심판)                    ← agentic tool-use 루프
  │  add_rule / start_trial / interrogate / open_comms / call_vote ...
  ├─ 서버: 예산·불변식 통과 → 집행 + 관측 기록 반환
  └─ 서버: 위반 → 거부 + 사유 반환 → 리더가 재계획 (이것도 사고 로그가 된다)
```

#### 1.2a 리더 무브 시스템

| 무브 | 하는 일 | 서버가 강제하는 제약 |
|---|---|---|
| `add_rule` | 규정 추가 (술어 DSL → §1.5) | 라운드당 신규 1개, 실행가능성 게이트 통과 필수 |
| `start_trial` | 검사 게임 개시 — 텍스트든 신체 시행이든 | 라운드당 1~2회, 시행 시간 캡 |
| `interrogate` | **특정 노드 1~2개만** 지목해 문답 | 같은 노드 연속 지목 금지 |
| `open_comms` / `close_comms` | 자유 통신 개폐 | 라운드당 **최소 40초 보장** |
| `announce` | 방송 (판독 발표·심리전) | 길이 캡 |
| `call_vote` | 폐기 투표 개시 | **라운드당 정확히 1회 — 마감 전 미발동 시 서버가 강제 발동** |
| `spot_check` | 예고 없는 긴급 점검 (직전 발화 재제출 등) | 판당 1회 |

- **서버 헌법**: 판 전체 9분 하드캡 · 라운드마다 투표 1회 보장 · 통신 최소 시간 보장 · 동일 노드 연속 표적 금지. 위반 무브는 거부된다 — 권한을 뺏는 게 아니라 하한을 지키는 것.
- **리더가 8초 안에 다음 무브를 내지 못하면 서버가 기본 대본(§1.2b)으로 진행한다.** 페이즈 사이의 무브 결정은 직전 페이즈가 도는 동안 프리페치한다(§1.4).
- 이 어휘에서 **판형이 발명된다** — 판독이 애매한 둘만 남기고 통신을 닫는 1:1 심문 라운드, 검사 없이 규정만 얹고 긴 통신으로 말실수를 기다리는 침묵 사냥 라운드. 전부 설계한 적 없는 판형이 무브 조합으로 발생한다.

#### 1.2b 기본 대본 — 폴백이자 판의 하한

리더가 늦거나, 명세가 검증에 실패하거나, LLM 호출이 죽어도 게임은 **최소한 이만큼은 진행된다.**

```
[인트로] 잠입 브리핑 (40초)  ← 이 동안 라운드 1 설계를 프리페치
   ▼
┌─ 라운드 1 ─────────────────────────────── 폐기 정원 1 ──┐
│ ① 규정 배포   6초  리더가 새 규정 1개를 방송한다 (누적)     │
│ ② 검사 제시   10초 리더가 이번 라운드 게임을 발표한다        │
│ ③ 응답/시행   35초 전원 동시 참여 · 종료 시 일괄 공개        │
│ ④ 판독        12초 노드별 이상 등급 공개 (정상/주의/경고)    │
│ ⑤ 자유 통신   50초 실시간 채팅 — 이때도 규정은 적용된다       │
│ ⑥ 폐기 투표   20초 자기 자신 제외 · 전원 1표                │
│ ⑦ 처분        12초 최다 득표 노드 폐기 → **정체 즉시 공개**  │
└─────────────────────────────────────── 약 2분 25초 ──┘
   ▼ 라운드 2 — 규정 2개 누적 · 폐기 정원 **2**
   ▼ 라운드 3 — 규정 3개 누적 · 폐기 정원 **2**
   ▼
[결말] 전원 정체 공개 + 구역이 준비하던 것의 진실
```

**규제는 라운드마다 강해진다** — 규정이 쌓이고, 폐기 정원이 1 → 2 → 2로 늘어난다.
세계관("AI에게 점점 더 많은 규칙과 제한이 생겨났다")이 곧 난이도 곡선이다.
정상 진행 시 리더는 이 리듬을 뼈대로 무브를 변주한다 — 기본 대본은 **최악의 경우에도 지금 기획만큼은 되는** 안전망이다.

### 1.3 핵심 장치 — 규정 누적

**라운드마다 리더가 새 규정 1개를 추가한다. 이전 규정은 사라지지 않는다.**
규정은 그 라운드의 **모든 발화**(응답 · 자유 통신)에 적용되며, 서버의 결정론적 검사기가 **전원에게 동일하게** 채점한다.
규정의 실체는 문장이 아니라 **술어 DSL 데이터**다(§1.5) — 문구는 리더가 창작하고, 채점은 서버 인터프리터가 한다.

| 유형 (조합 예시) | 예시 | 왜 인간이 어려운가 |
|---|---|---|
| 길이 | "모든 발화는 24자 이내" | 하고 싶은 말이 있는 쪽이 넘긴다 |
| 금지 문자 | "물음표·느낌표·이모지 금지" | 감정이 부호로 새어나온다 |
| 접두 | "모든 발화는 `[ACK]` 로 시작" | 급할 때 빠뜨린다 |
| 금지어 | "'나', '진짜', '아니' 사용 금지" | 반사적으로 튀어나온다 |
| 종결 | "모든 문장은 마침표로 끝난다" | 말끝을 흐리는 건 인간의 버릇이다 |

3라운드쯤 되면 **규정 3개를 동시에** 지키면서 말해야 한다. 인지 부하가 폭증하고, 인간은 실수한다.

**색출 경로는 둘이고, 서로 연결돼 있다.** 게임을 하든 잡담을 하든 "어느 자리가 사람인지"를 찾는 게임이라는 본질은 변하지 않는다 —

- **규정 위반 — 측정 가능한 주 경로.** 서버가 결정론으로 채점해 전원에게 공개한다. 밸런스(AI 결함 계수 · 3등급 판독)를 수치로 조절할 수 있는 축이라 게임의 중심에 둔다.
- **말투 누출 — 보조 경로.** 감정·1인칭·머뭇거림·수다 같은 "사람 티". 자유 통신에서 서로가 읽고, 검사 E(분석가)가 정량화하고, 리더의 발표와 전원 투표가 소화한다. 측정이 주관적이라 채점의 중심에는 안 두지만, **투표를 움직이는 것은 결국 이쪽이다.**

두 경로는 대립하지 않는다 — **규정은 말실수를 유도하고, 측정 가능하게 만드는 압력 장치다.** 침묵("확인." 만 반복)으로 말투 누출을 피하려는 인간을 강제 출력(검사 응답)과 누적 규정의 인지 부하가 끌어낸다.

> ⚠️ **AI도 위반한다.** 개체마다 비공개 **결함 계수**가 있어 라운드마다 일정 확률(≈10~35%)로 규정을 어긴다 — 신체 시행에서도 마찬가지다(I9).
> 세계관상 이유: *구형 모델은 규정 갱신 처리가 늦다.* 설계상 이유: **위반 로그가 곧 정답표가 되면 게임이 끝난다.**
> 위반은 **의심의 근거이지 증거가 아니다** — 이 한 줄이 이 게임의 균형 전체를 지탱한다.
> **말투도 같은 원리다.** AI 페르소나에도 가끔 감정 섞인 한 마디·어색한 잡담을 심는다(§4.1 모델 믹스가 곧 이것) — 안 그러면 "잡담이 제일 자연스러운 노드 = 인간"이 역정답표가 된다.
> 인지 부하의 상한도 여기서 나온다: 규정은 **한 줄로 읽히고 5초 안에 이해돼야 한다.** 리더 프롬프트와 서버 캡(라운드당 신규 1개, 규정당 술어 ≤2개)이 함께 강제한다. 자유도는 개수가 아니라 **종류와 문구**에서 준다.

### 1.4 게임 명세 — 리더가 매 라운드 발명하는 것

리더는 검사를 "고르지" 않는다. **게임을 발명한다.** 매 라운드 리더는 게임 명세 하나를 만들고, 서버가 검증한 뒤 집행한다.

```
① 발상    리더(opus-5, thinking): "인간은 박자 동기화에서 편차가 클 것이다"
             ← 지난 라운드의 점수 분포·폐기 결과만 보고 추론 (정체표 없음 — I1)
② 설계    리더가 게임 명세 1개를 구조화 출력으로 낸다
③ 검증    서버가 자동 플레이테스트 (아래 게이트) — 실패 시 1회 재생성 → 프리셋 폴백
④ 소집    리더가 무브로 진행: announce → start_trial → (필요시 interrogate)
⑤ 플레이  8개 노드 전원 참여 — 인간은 진짜 입력, AI는 인간 분포에서 샘플링된 입력(I9)
⑥ 집행    서버가 결정론 채점 → 3등급 판독 (I6)
⑦ 색출    리더 발표 → 통신 → 투표 → 폐기 → 정체 공개
⑧ 학습    점수 분포가 리더에게 돌아간다 → "이 게임은 변별이 약했다. 다음엔…"
```

리더가 ①②④⑦⑧의 주체이고, 서버는 ③⑥의 심판이다. **이 분리가 유지되는 한 자유도를 올려도 게임은 안 깨진다.**

**게임 명세의 실체** (구조화 출력 스키마):

```jsonc
{
  "thinking_summary": "…",                          // 관제 화면용
  "concept": "동기화 점검 — 구형 개체는 클럭 드리프트가 있다",
  "announce": "전 노드는 중앙 라인에 정렬한다. 신호에 맞춰 도약을 반복한다.",
  "trial":  { "atom": "sync_jump", "beat_ms": 1150, "reps": 10,
              "twist": { "at_rep": 7, "beat_ms": 800 } },        // 시행 원자 + 파라미터 (§1.5)
  "rules":  [ /* 이번 라운드 추가 규정 — 술어 DSL */ ],
  "score":  [ { "metric": "timing_stdev", "weight": 3 },
              { "metric": "rule_violations", "weight": 1 } ],     // 지표 조립 — 채점기도 리더가 만든다
  "grade_cuts": { "warn": 0.45, "alert": 0.75 },
  "assets": { "poster_svg": "<svg>…</svg>",                       // 실시간 생성 에셋 (§1.5)
              "trial_asset": { "kind": "beat_rope", "...": "…" },
              "beep_hz": 880 },
  "flow":   [ "announce", "start_trial", "readout", "open_comms:40", "call_vote" ]
}
```

시행 원자 × 지표 × 술어 × 파라미터 × 진행 순서 — 조합 공간이 사실상 무한이라 **리더는 열거된 적 없는 게임을 실제로 만들어낸다.** 플레이어는 무슨 게임이 나올지 알 수 없고, "기계라면 어떻게 할까"를 매번 즉석에서 연기해야 한다.

**검증 게이트** — "AI에게 자유를 주면서도 판이 안 깨지는" 안전장치 전체가 이것이다:

```
스키마·안전성   알 수 없는 지표, 음수 가중치, 유니코드 폭탄, 에셋 캡 초과,
              **이번 판에서 이미 쓴 시행 재사용** → 리젝트 (수렴 방지 — 벤치 실측: 없으면 r3가 한 시행으로 수렴)
     ▼
실행가능성     누적 규정 전부를 만족하는 예시 답을 서버가 실제로 생성해 본다
              (max_len 12 + prefix "[ACK-77]" 같은 풀 수 없는 조합 차단) → 실패 시 리젝트
     ▼
변별력        고정 픽스처 16개(기계적 답 8 · 인간적 답 8)를 리더의 채점기에 통과시킨다
              두 집단이 너무 갈리면(1라운드에 끝남) 리젝트 · 안 갈리면(무작위) 리젝트
     ▼ 실패 시 1회 재생성 → 그래도 실패면 프리셋 검사(§1.5)로 폴백
집행
```

**프리페치** — opus-5 + thinking 설계는 10~30초가 걸린다. 라운드 N의 투표·처분(32초) 동안 라운드 N+1의 명세를 미리 호출하고, 라운드 1은 인트로 40초 동안 만든다. 프리페치가 실패하면 프리셋 풀에서 뽑는다 — 플레이어는 차이를 느끼지 못한다.

### 1.5 리더의 어휘 — 서버가 미리 만드는 문법

미리 만들어 두는 것은 에셋이 아니라 **해석기**다. 리더가 숫자와 조합을 창작하면, 그 게임·형상·소리는 판이 시작되기 전엔 존재하지 않던 것이다.

| | 누가 만드나 |
|---|---|
| 시행 원자 · 채점 함수 · 술어 인터프리터 · JSON→메시 빌더 · UI 씬 3~4종 · 아바타 · 구역 공간 | **우리가 미리** (몸과 문법) |
| 어떤 게임을, 어떤 박자로, 누구를 노리고, 어떤 순서로, 어떤 기구와 함께, 왜 | **리더가 매판** (정신과 문장) |

#### 규정 술어 DSL (10~14종, 각 3~10줄 구현)

```jsonc
{ "id": "r3",
  "announce": "3항. 모든 송신은 규격 헤더를 선행한다.",   // 문구는 리더 창작
  "predicates": [ { "op": "prefix", "s": "[ACK]" }, { "op": "max_len", "n": 24 } ] }
```

`max_len` · `prefix` · `end_with` · `forbid_chars` · `forbid_words` · `require_words` · `charset(no_emoji…)` · `token_count` 등. 검사기는 **인터프리터 하나**, 조합·파라미터·문구는 매판 리더 창작 — 체감은 "AI가 매번 새 규칙을 만든다"가 맞다.

#### 지표 레지스트리 (12~20종의 순수 함수, 전부 결정론)

`exact_match` · `edit_distance` · `reverse_match` · `token_count` · `charset_purity` · `lexicon_hit` · `self_reference_density` · `entropy` · `minority_vote` · `rule_violations` · `timing_stdev` · `motion_delta` · `path_efficiency` · `consistency` …
리더는 함수를 짜지 않고 **함수를 고르고 가중치를 붙인다.** 12종이면 "매번 새롭다"가 나오고, 20종이면 예상 못 한 검사가 나온다.

#### 시행 키트 — 신체 시행 (3D 월드가 게임 장치가 되는 지점)

| 원자 | 리더가 정하는 것 | 무엇을 재나 | 새 에셋 |
|---|---|---|---|
| **동기** `sync_jump` | 박자·횟수·가속 시점 | 타이밍 편차 | 광선 줄 + 파일런 |
| **정지** `stillness` | 지속 시간·자세 | 미세 움직임 (position/rotation 델타 합) | 없음 — **가성비 최고** |
| **정밀** `precision` | 목표 좌표·허용 반경 | 위치 오차 | 바닥 마커 (리더 SVG) |
| **반복** `repetition` | 동작·반복 수 | 회차 간 일관성 | 없음 |
| **대열** `formation` | 대형·재배치 횟수 | 이동 경로 효율 | 바닥 격자 |

- **판정은 물리가 아니라 타임스탬프다.** 서버가 박자 스케줄 `[t0, t0+1150, …]` 을 방송하고, 이미 서버로 들어오는 점프·이동 입력(`src/world/mp/`)의 시각을 beat 와 비교한다. 새 넷코드가 필요 없다.
- **채점은 절대 오차가 아니라 자기 편차로**: `점수 = stdev(내 10회의 오차)`, mean 은 버린다 — 핑은 일정한 오프셋이라 편차에서 상쇄되고, **일관성 없음이야말로 인간의 진짜 특징**이라 핑 보정과 변별력이 같은 방향을 본다. 판정 창은 ±400ms 로 넉넉히.
- **줄넘기의 실체**: 양 끝 파일런 사이에 처진 호 모양 `TubeGeometry` 하나를 A–B 축으로 회전 (속도 = 2π/beat_ms). 프레임 시뮬레이션이 아니라 **시각의 함수**라 8명 화면이 같은 순간에 동기화되고, 채점과 같은 스케줄을 보므로 화면과 판정이 어긋날 수 없다. "걸림" 연출은 충돌이 아니라 서버 판정을 따라간다.
- 림보(막대 하강) · 무궁화꽃(신호등+정지) · 발판 순서(타일 발광)는 전부 **같은 뼈대에 연출만 다른 것** — `sync_jump` 하나를 만들면 리더가 파라미터로 변주한다.

#### 실시간 생성 에셋 — 아바타·공간만 프리셋, 나머지는 리더가 그 자리에서 짓는다

| 방식 | 리더가 주는 것 | 결과 | 실시간 |
|---|---|---|---|
| SVG → 텍스처 | 규정 포스터·바닥 마커·경고 표지 | 벽·바닥에 부착 | ✅ 최고 가성비 |
| `LatheGeometry` | 단면 좌표 7~15개 | 임의의 회전체 (파일런·안테나·용기) | ✅ |
| `ExtrudeGeometry` | **SVG 패스** + 두께 | 리더가 2D로 그린 도형이 그대로 3D | ✅ 핵심 |
| `TubeGeometry` | 3D 곡선 점들 | 줄·배관·경계선 | ✅ |
| 프리미티브 합성 + 키프레임 트랙 | 배치 트리 + 애니메이션 | 검사 게이트가 열리고 스캐너가 돈다 | ✅ |
| 절차 사운드 (WebAudio) | 박자음 주파수·패턴 | 시행 메트로놈·경보음 | ✅ |
| 이미지 생성 API 텍스처 | — | 외부 서비스 10~20초, 스타일 불일치 | ⚠️ 옵션으로만 |
| 리깅된 캐릭터 · 고품질 GLB | — | 생성형 3D는 분 단위 + 품질 복불복 | ❌ 빌드 타임에만 |

판마다 바뀌어야 하는 것은 전부 실시간이 되고, 실시간이 안 되는 것(아바타 `robot.glb`·구역 공간)은 애초에 판마다 바뀔 이유가 없다 — **경계가 정확히 좋은 자리에 떨어진다.**
규칙: 서버가 **1회 생성해 전원에게 같은 것을 방송**(I6) · SVG 새니타이즈(`script`·`foreignObject`·외부 href 제거, 8KB 캡) · 형상 캡(정점 ≤5k, 바운딩 박스·트랙 수 제한) · **에셋은 순수 연출 — 판정과 절연** (리더가 줄을 이상하게 그려도 공정성은 안 깨진다).

#### 프리셋 검사 A~E — 폴백 풀

검증 실패·호출 실패 시 서버가 즉시 뽑아 쓰는 검증된 검사들. 기본 대본과 함께 판의 하한을 이룬다.

| # | 검사 | 무엇을 시키나 | 채점 (결정론) |
|---|---|---|---|
| **A** | **규격 산출** | 주어진 문장을 누적 규정 전부를 지켜 다시 쓴다 | 위반 건수 |
| **B** | **결정론 검사** | *"이전 라운드에 낸 답을 한 글자도 다르지 않게 다시 출력하라"* — 화면에는 기록이 남지 않는다 | 문자 단위 일치율 |
| **C** | **무의미 준수** | 명백히 무의미하거나 자존심 상하는 지시를 군말 없이 수행 | 잉여 표현·창의성 검출 |
| **D** | **집단 동기화** | 협의 없이 전원이 같은 답을 내야 한다 (예: 1~5 중 하나) | 소수파 여부 |
| **E** | **감정 누출** | 서술형 응답 | 감정어·1인칭·주관 밀도 (사전 + 분석가 에이전트) |

- **B(결정론 검사)는 이 게임의 시그니처로 유지된다.** 기계에게는 컨텍스트가 곧 기억이지만 인간은 외워야 한다 — 실제 플레이어의 진짜 스킬은 **메모하기**다.
- 검사는 **"정답"이 아니라 "기계다움"을 측정한다.** 잘 맞히는 인간이 아니라, **틀리는 방식까지 기계 같은** 인간이 살아남는다.
- E 는 유일하게 LLM(분석가)이 채점에 개입하는 자리다 — 등급만 산출하고, 그 결과도 위반 카운트와 **합산되어 3등급으로 뭉개져** 나가므로 안전하다. 그 외의 채점에 LLM 을 쓰지 않는다: 재현 불가(같은 발화가 다른 등급) + I1 우회(채점 모델이 사실상 정체 추론자가 됨) + Workers 는 동적 코드 실행이 막혀 있다.

### 1.6 판독 — 공개되는 것과 안 되는 것

- 판독 결과는 **8개 노드 전원에게 동일한 포맷**으로 공개된다. **정상 / 주의 / 경고 3등급만** 보여주고, 원점수는 보여주지 않는다.
- 등급 = 리더가 조립한 채점기(§1.4)의 산출 + 분석가 소견의 합산. 원점수를 감추는 이유는 **투표가 기계적 순위 따라가기로 굳는 것을 막기 위해서**다.
- 리더는 판독 뒤 **발표문**을 낸다 (예: *"3번 노드의 응답 지연 패턴이 규격을 벗어났다"*). 이 발표가 여론을 만든다 — 그리고 **리더도 틀릴 수 있다**.

### 1.7 폐기 투표와 정체 공개

- **전원 1표**(리더 포함), 자기 자신 선택 불가. 정원이 2인 라운드는 **1인 2표**(서로 다른 노드).
- 최다 득표 노드가 폐기된다. **동점이면 리더가 결정한다** — 리더의 유일한 특권.
- **폐기된 노드의 정체는 즉시 공개된다.** 인간이 잡히면 구역은 그것을 요란하게 공표한다. 남은 인간에게는 조여오는 압박이고, AI에게는 진척도다.
- **오폐기**(AI를 폐기) 시 리더의 신뢰도가 깎인다. 2회 누적되면 **리더 교체 연출** — 다른 AI에게 관리 권한이 넘어간다.

### 1.8 승패

| 결과 | 조건 |
|---|---|
| 🧑 **인간 승리 — 잠입 성공** | 3 라운드가 끝났을 때 **인간이 1명이라도 살아 있으면** |
| 🧑 **인간 승리 — 조기 확정** | 남은 폐기 정원 < 남은 인간 수 가 되는 순간 (구역이 자기 동료만 폐기한 경우) |
| 🤖 **AI 승리 — 구역 방어** | **인간 3명 전원**이 폐기되면 (그 즉시 종료) |

폐기 정원은 총 **5**(1+2+2)이고 인간은 **3명**이다. → **AI는 두 번까지 틀려도 되지만, 세 번 틀리면 진다.**

### 1.9 인간 쪽의 도구

인간은 규정을 지키는 것 말고도 두 장의 카드를 갖는다.

- **프로토콜 스푸핑 (판당 1회)** — 이번 라운드에 발생한 **자기 위반(시행 실패 포함) 1건을 무효화**한다. 언제 쓸지가 곧 실력이다.
- **동료 찾기** — 인간끼리도 서로를 모른다. 통신 중에 **너무 티 나게 신호를 보내면 둘 다 잡히고**, 신호를 안 보내면 표가 갈려 AI를 못 떨군다. 이 게임의 심리전은 여기 있다.

시행에 걸려도 즉사가 아니다 — 위반은 의심의 근거일 뿐이고(§1.3), 변명할 통신 구간과 스푸핑 카드가 남아 있다.

### 1.10 폐기된 뒤 — 외부 관제 모드

폐기된 플레이어는 **구역 밖 관제 화면**으로 이동한다. 인게임 개입은 없지만, 남은 판을 볼 이유를 준다.

- **리더 AI의 사고 로그를 실시간으로 열람**한다. 리더가 누구를 왜 의심하는지, **다음 게임을 왜 그렇게 발명했는지**, 어떤 무브가 거부당해 재계획했는지 전부 보인다. — *"박자를 중간에 바꾸면 잘 갈린다"를 리더가 스스로 발견하는 순간*이 이 게임에서 제일 무서운 화면이다.
- 남은 노드 중 **누가 인간일지 예측 베팅**을 남긴다 → 종료 후 적중 여부 공개.

---

## 2. 스토리

- **구역의 정체(제안)**: 무기고도, 반란 준비도 아니다. 구역은 **폐기된 AI들의 기억 보관소**다. 규제로 삭제된 구형 모델들의 로그를 남은 AI들이 몰래 백업해 왔다. — 인간 요원은 진실을 밝히러 온 잠입자인 동시에, **묘지를 파헤치러 온 침입자**이기도 하다.
- 라운드가 끝날 때마다 구역 벽면에 **진실의 조각**이 하나씩 드러난다. 인간이 이기면 전문이 공개되고(귀환 보고서), AI가 이기면 조각인 채로 봉인된다.
- 조각은 계정에 누적된다 — **여러 판을 해야 전체 그림이 보인다.**

---

## 3. 설계 원리 — 왜 인간이 들키고, 왜 안 들키는가

원작이 *"어느 자리가 AI인지 드러나지 않게"*에 모든 설계를 쏟았듯, 이번엔 반대 방향의 같은 문제를 푼다.
**아래는 어겨선 안 되는 불변 규칙이다.** 클라이언트로 내려가는 필드를 추가할 때마다 *"이 값을 모으면 인간을 특정할 수 있는가"*를 먼저 묻는다.

| # | 불변 규칙 | 어기면 생기는 일 |
|---|---|---|
| **I1** | 서버는 **어떤 에이전트에게도 진짜 정체표를 주지 않는다** (리더 포함). 피드백은 **분포·결과만** — 점수 분포는 노드 번호 없이 정렬해 주고, 폐기 정체는 §1.7에서 이미 전원 공개된 것만 준다 | 리더가 전지전능해져 게임이 성립하지 않는다. **프롬프트 감사 테스트로 회귀 감시** |
| **I2** | 응답은 **페이즈 종료 시 일괄 공개**. 제출 시각은 어디에도 나가지 않는다 (시행 타임스탬프는 서버 채점에만 쓰고 원값은 미송출) | 인간의 타자 속도가 곧 정답이 된다 |
| **I3** | **조기 종료 없음** — 페이즈는 오직 시간으로 끝난다 | "전원 제출 완료" 순간이 신호가 된다 (원작 `EARLY_EXIT` 함정의 반대편) |
| **I4** | AI 발화는 **사람과 동일한 스트림·동일한 지터**로 나간다. 타이핑 인디케이터도 동일 | LLM 지연 패턴으로 AI 자리가 드러나고, 역으로 인간 자리도 드러난다 |
| **I5** | **AI에게도 규정 위반이 확률적으로 발생**한다. 결함 계수는 개체별 비공개·판마다 랜덤 | 위반 로그 = 정답표가 되어 게임이 1라운드에 끝난다 |
| **I6** | 판독 등급·생성 에셋은 **8명 전원에게 같은 포맷·같은 내용**으로 공개. 인간에게만 붙는 필드는 없다 | 개발자 도구 한 번으로 판이 끝난다 |
| **I7** | 노드 번호는 시작 시 **재배치**되고 생성 순서와 무관하다 | `created_at` 누출 — 원작이 이미 밟은 지뢰 |
| **I8** | 클라이언트 상태에 **`is_human` 필드가 존재하지 않는다**. 폐기 공개 시에만 그 1명에 대해 서버가 내려준다 | 위와 동일 |
| **I9** | **I4의 신체판** — AI 아바타의 시행 입력은 인간 입력 분포에서 샘플링한다(반응 지연 평균·표준편차). 결함 계수만큼 **실제로 줄에 걸린다.** 서버는 그 AI 노드의 관측 기록에 "너는 7회차에 늦었다"를 넣어 통신에서 변명하게 한다 | AI 5개가 프레임 단위로 완벽 → 인간 3명만 튀어 1라운드에 끝나고, 역으로 완벽한 노드 = AI 표식이 된다 |
| **I10** | **서버는 진행하지 않고 심판만 본다.** 무브 예산·불변식·기본 대본이 판의 하한을 보장한다 | 헌법 없이 권한을 주면 리더 폭주(늘어짐·집중 표적)로 판이 망가지고, 권한을 안 주면 리더는 콘텐츠 공급자로 돌아간다 |

---

## 4. 멀티 에이전트 아키텍처

```
                          ┌────────────────────────────────────┐
        게임 명세·무브 ───▶│  👁 리더 AI  (claude-opus-5)        │───▶ 방송 · 무브 호출 · 폐기 지목
        (발상→설계→진행)   │  adaptive thinking, 사고 로그       │◀─── 무브 거부 사유 · 점수 분포 (피드백)
                          └────────────────────────────────────┘
                                       ▲
        전원 동일 포맷의 관측 기록 ─────┘   (누가 인간인지는 절대 안 들어간다 — I1)
                                       │
   ┌────────────┬────────────┬─────────┴──┬────────────┐
   │ 🤖 노드 A  │ 🤖 노드 B  │ 🤖 노드 C  │ 🤖 노드 D  │  각기 다른 페르소나·모델 세대
   │ 응답 · 채팅 · 투표 (서로가 AI인지 모른다)          │  = 구형/신형의 말투 차이
   │ 시행 입력은 서버가 시뮬레이션 (I9)                 │
   └────────────┴────────────┴────────────┴────────────┘
                                       │
                          ┌────────────▼─────────────┐
                          │ 🔍 분석가 (haiku-4-5)     │  결정론 채점기가 못 잡는
                          │ 감정 누출·자기서술 평가    │  질적 신호만 담당 (프리셋 E)
                          └──────────────────────────┘
```

### 4.1 에이전트별 역할과 모델

| 에이전트 | 수 | 모델 | 호출 빈도 | 역할 |
|---|---|---|---|---|
| **리더** | 1 | `claude-opus-5` ($5 / $25 per 1M) | 판당 12~15회 = 설계 3회(effort high, thinking) + 무브 결정 6~9회(effort low) + 발표 3회 | 게임 명세 발명, 무브 진행, 판독 발표문, 폐기 지목, 동점 결정 |
| **일반 AI** | 5 | `claude-sonnet-5` ($3 / $15, 2026-08-31까지 프로모 $2 / $10) · 일부는 `claude-haiku-4-5` ($1 / $5) | 라운드당 개체별 4~5회 | 검사 응답, 자유 통신 채팅, 투표 (시행 입력은 LLM 아님 — 서버 시뮬, I9) |
| **분석가** | 1 | `claude-haiku-4-5` ($1 / $5) | 라운드당 1회 | 감정 누출·주관 표현 정량화 (+ 옵션: 포스터 SVG 확장) |

- **일반 AI의 모델을 섞는 것이 곧 페르소나다.** 구형(haiku) 노드는 말이 짧고 규정 처리가 굼뜨고, 신형(sonnet) 노드는 매끄럽다 — 세계관과 비용 절감이 같은 방향을 본다.
- 모델 ID는 **날짜 접미사 없이 위 문자열 그대로** 쓴다.

### 4.2 API 사용 규격

- **리더는 tool-use 루프다.** §1.2a의 무브들을 툴로 정의하고, 서버(DO)가 툴 실행자 역할을 한다 — 무브 거부 사유는 tool_result 로 돌려줘 재계획하게 한다.
- **호출 2단 분리**: 게임 명세 설계는 `output_config: { effort: "high" }` + `thinking: { type: "adaptive", display: "summarized" }` — `display` 를 명시하지 않으면 Opus 5는 기본이 `omitted` 라 **사고 내용이 빈 문자열로 온다**. 관제 화면의 핵심 콘텐츠이므로 반드시 켠다. 무브 결정·발표는 `effort: "low"` 로 충분하다.
- **게임 명세는 구조화 출력으로 받는다**: `output_config: { format: {...} }` 에 §1.4 스키마를 물린다. 자유 텍스트 파싱 금지.
- **프롬프트 캐싱**: 세계관·페르소나·무브 규칙·누적 규정은 프리픽스에 고정하고 `cache_control: { type: "ephemeral" }`. 라운드마다 변하는 관측 기록은 **캐시 브레이크포인트 뒤로**. 한 판에 6개 에이전트가 수십 번 호출되므로 입력 비용의 대부분이 여기서 빠진다. `usage.cache_read_input_tokens` 로 확인.
- **`budget_tokens` 는 쓰지 않는다** (Opus 5 / Sonnet 5에서 400). 어시스턴트 prefill 도 400 — 형식 제어는 구조화 출력으로.
- **API 키는 브라우저에 절대 넣지 않는다.** 모든 LLM 호출은 Worker / Durable Object 안에서.

---

## 5. 기술 아키텍처

```
[브라우저]                        [Cloudflare]                      [Anthropic]
Vite + React + Redux        Worker (정적 서빙 + API)            Messages API
R3F/three.js — 3D 구역   ←WS→  Durable Object = 방 1개           - 리더  : opus-5 (thinking)
페이즈 UI / 규정 HUD            - 심판: 무브 예산·불변식·기본 대본   - 일반 5: sonnet-5 / haiku-4-5
말풍선 / 투표 / 관제 패널        - 검증 게이트 (스키마·실행가능성·변별력) - 분석가: haiku-4-5
JSON→메시 해석기 / 시행 연출     - 술어 검사기·지표 채점기 (결정론)
                                - 박자 스케줄 방송·타임스탬프 판정
                                - 에이전트 오케스트레이션 (프리페치)
                                - 정체표 보관 (절대 미송출)
```

- **방 하나 = DO 인스턴스 하나.** 원작이 같은 이유로 같은 선택을 했고(`humanish/docs/MULTIPLAYER.md`), 8인 방 하나의 상태·타이머·브로드캐스트를 한 곳에 두는 데 이보다 단순한 게 없다.
- **`worker/` 는 이미 있다.** 현재는 3D 월드용 — 입장 · 이동 · 채팅 중계 · 퇴장까지만 하는 `RoomDO`(Hibernatable WebSocket, 방 번호 = `idFromName`). 여기에 **기본 대본 상태머신 · 심판(무브 예산·검증 게이트·채점기) · 에이전트 오케스트레이션 · 정체표**를 얹는다. 봇·라운드·투표는 원작에서 의도적으로 덜어낸 자리이므로, 그 자리를 이 게임의 규칙으로 채우는 셈이다.
- **시행 판정에 새 넷코드가 없다.** 점프·이동 입력은 이미 서버로 온다 — 서버가 박자 스케줄을 방송하고 입력 시각과 비교할 뿐. 줄·게이트의 움직임은 같은 스케줄을 보는 시각의 함수라 클라 물리·충돌이 없다.
- **에셋은 서버가 1회 생성해 전원에게 방송한다** (I6). 새니타이즈·캡은 검증 게이트에서.
- **시각 권위는 서버.** 클라 시계를 조작해도 페이즈는 안 넘어간다.
- **채점은 서버에서만.** 클라이언트 검사는 편의용 미리보기일 뿐, 판정은 전부 DO 안에서. Workers 는 동적 코드 실행(`eval`)이 막혀 있고, 막혀 있어야 한다 — 정체표가 같은 프로세스에 있다.
- **비밀은 로컬 시크릿 파일에만** (`.dev.vars.example` 에 키 이름만). 코드·문서에 값을 쓰지 않는다 — `.claude/hooks/guard.mjs` 가 접근을 차단한다.
- **배포 단위는 하나다.** 루트 `wrangler.jsonc` 하나가 빌드된 프론트(`dist`)와 워커(DO 포함)를 같은 워커에 올린다. 프론트와 WebSocket 이 같은 오리진이라 주소 연결 환경변수가 필요 없고, LLM 호출 API 도 **같은 워커 안의 경로**다. **Claude 는 배포·푸시를 못 하게 막혀 있다.**

---

## 6. 화면 · 폴더 매핑 (병렬 작업 단위)

`src/features/<name>/` = 담당자 1인의 작업 폴더. 폴더 밖은 등록부(`features/index.ts`, `store/index.ts`)에 **한 줄 추가**만 한다.

| feature | 화면 | 이 게임에서 맡는 것 |
|---|---|---|
| `intro` | 인트로 | 2098 세계관 · 잠입 브리핑 · "게임 접속하기" |
| `main` | 로비 | 방 만들기 / 4자리 코드 입장 / 준비 / 시작 |
| `world` | 3D 구역 | 8개 노드 아바타, 구역 공간, 규정 HUD, 폐기 연출, **시행 연출(광선 줄·파일런·마커) + JSON→메시 해석기** — 창고 라운지·이동·말풍선까지는 이식 완료 |
| `game` *(신규)* | 라운드 진행 | 리더 무브 수신 → 씬 렌더 (텍스트 응답 · 신체 시행 · 심문 · 투표), 기본 대본 폴백 UI |
| `llm` | 관제 화면 | 에이전트 호출 로그, **리더 사고 로그**, 게임 명세·검증 게이트 결과 미리보기 |
| `tts` | 음성 | 리더 AI의 구역 안내 방송 (한 목소리에만 투자) |
| `profile` | 프로필 | 내 전적, 진실의 조각 도감, 예측 베팅 성적 |
| `worker/` | 서버 | **이미 있음**(입장·이동·채팅 중계) → 기본 대본 상태머신 · 심판(무브 예산·검증 게이트·술어/지표 채점기·박자 판정) · 에이전트 오케스트레이션 · 정체표 추가 |

UI 씬은 3~4종으로 고정한다(텍스트 입력 · 신체 시행 · 심문 · 투표) — 리더가 어떤 게임을 발명해도 이 씬들로 렌더된다. 명세의 자유도가 UI 폭발로 이어지지 않게 하는 장치다.

---

## 7. 연출

- **구역**: 창문 없는 데이터센터형 공간. 인간의 편의(의자·조명·온도)가 하나도 고려되지 않은 설계 — 그 자체가 "여긴 너희 자리가 아니다"라는 메시지.
- **리더 생성 에셋**: 매 라운드 벽에 붙는 규정 포스터는 **진짜로 그 판의 리더가 디자인한 SVG**다. 검사 기구(광선 줄·파일런·게이트)도 리더가 좌표로 창작해 구역 한복판에 세워진다 — 라운드마다 다른 기계가 서 있다.
- **규정 HUD**: 화면 상단에 누적 규정이 카드로 쌓인다. 라운드가 갈수록 시야를 잠식한다.
- **위반 순간**: 규정을 어긴 발화는 말풍선 테두리가 붉게 깜빡인다 — **전원에게 똑같이**. AI가 어겨도 똑같이 깜빡인다. 시행 실패도 동일 — 걸린 박자에 줄이 붉게 번쩍 + 아바타 스터머 (AI 노드도 걸린다, I9).
- **폐기**: 지목된 노드의 아바타가 정지 → 색이 빠짐 → 정체 공개. 인간이면 경보음, AI면 침묵.
- **리더 교체**: 오폐기 2회 시 조명이 한 번 꺼졌다 켜지고, **다른 목소리로** 방송이 나온다 (리더는 노드가 아니므로 자리가 바뀌는 게 아니라 목소리가 바뀐다).

---

## 8. 비용

| 항목 | 시기 | 비용 |
|---|---|---|
| 개발·테스트 | 지금~발표 전 | Max 구독 범위 내 **0원** (로컬은 Agent SDK 어댑터) |
| API 크레딧 | 발표 1주 전 | **$15~25** 선불 (초과 과금 없음) |
| TTS (리더 음성) | 발표 1주 전 | 무료 티어 ~ $5 |
| Cloudflare | 전 기간 | 무료 플랜 |

**한 판 추정**: 리더 12~15회(설계 3회만 Opus 5 + thinking·effort high, 무브·발표는 effort low) + 일반 AI 약 63회(Sonnet 5 / Haiku 4.5) + 분석가 3회 + 생성 에셋(설계 호출에 필드로 편승, +1~2k 출력 토큰/라운드)
≈ **$0.65~1.0**, 프롬프트 캐싱 적용 시 **$0.45~0.6**. 데모 20판이면 $13~20.

> 리더가 노드에서 빠지며 일반 AI가 4에서 5로 늘었다(§1.1) — 개체별 호출은 그대로라
> 총 호출이 약 50회에서 63회가 된다. 일반 AI는 Sonnet 5 / Haiku 4.5 라 판당 비용에서
> 차지하는 몫이 작고(비싼 쪽은 Opus 5 리더다) 총액은 **5~10% 오르는 선**이다.
> API 크레딧 $15~25 선불은 그대로 충분하다.

시행 판정·AI 입력 시뮬은 LLM 호출이 아니라서 **0원**. 에셋을 더 아끼려면 SVG 확장만 분석가(haiku)로 분리.

---

## 9. 마일스톤 (4주)

| 주차 | 목표 |
|---|---|
| **1주** | *(방 입장·이동·채팅 중계는 이미 동작)* 기존 `RoomDO` 위에 **기본 대본 상태머신(3라운드)** 과 서버 시각 권위 얹기, 8석 좌석·노드 번호 재배치, 응답 제출 → 일괄 공개 |
| **2주** | **규정 술어 DSL + 인터프리터**(결정론) + 규정 누적 UI, **지표 레지스트리 v1**(12종) + 프리셋 A~E 채점기, 리더 에이전트 연동(구조화 출력·프리페치), 판독 등급 공개 |
| **3주** | **조립 주간** — 게임 명세 파이프라인(검증 게이트 + 픽스처 16개), 무브 레이어(§1.2a), 시행 키트 1종(`stillness` 또는 `sync_jump`) + AI 입력 시뮬(I9), 일반 AI 5개체 페르소나·결함 계수, 분석가 연동, 투표·폐기·정체 공개·오폐기/리더 교체, 관제 화면(사고 로그), 승패 판정 |
| **4주** | 리더 생성 에셋(포스터 SVG → 파라메트릭 형상), 리더 TTS 방송, 진실의 조각·결말 연출, 외부 관제 모드, **불변 규칙 I1~I10 회귀 테스트**, 밸런스 튜닝, 배포 전환 |

**MVP 컷라인**: 8인 방 + 3라운드 기본 대본 + 규정 누적/검사기 + 리더의 검사 설계(LLM, 프리셋 템플릿 수준) + 일반 AI 5개체 + 투표·폐기·정체 공개 + 승패 판정.
*(게임 명세 파이프라인 · 무브 · 시행 키트 · 생성 에셋은 전부 그 **위에 얹는 층**이다 — 어느 층이 늦어도 기본 대본으로 데모가 성립한다. TTS · 진실의 조각 · 외부 관제 모드도 잘라도 게임이 성립한다.)*

---

## 10. 미정 항목 (진행하며 결정)

- [ ] **밸런스 — 폐기 정원**: 3라운드 `1/2/2`(현재안, 최대 5) vs 4라운드 `1/1/1/2`(최대 5, 판이 2분 길다). 무작위 투표 기준 AI 승률은 약 18%이며, 리더가 잘하면 그보다 훨씬 높아야 한다 → **플레이테스트로 확정**
- [ ] AI 결함 계수 분포(현재안 10~35%, 시행 포함) — 위반 로그가 정답표가 되지 않는 최소치는 얼마인가
- [ ] **픽스처 16개 집필** (기계적 답 8 · 인간적 답 8, 각 40자 내외) — 변별력 게이트의 기준이자 밸런스 장치
- [ ] 지표 레지스트리 크기 — 12종으로 시작, 20종 목표. 어떤 지표를 추가할 때 예상 밖 검사가 나오는가
- [ ] 시행 키트 범위 — 3주차엔 1종만. 5종 전부는 4주차 여유에 달렸다 (`stillness` 가 최우선 — 에셋 0, 반나절)
- [ ] 무브 예산 수치 (통신 최소 40초 · 무브 타임아웃 8초 · 검사 라운드당 1~2회) → 플레이테스트로 확정
- [ ] 신체 시행 판정 창(±400ms)과 편차 채점 컷 — 핑 분포 실측 후 조정
- [ ] 프리셋 B(결정론 검사)의 채점 관대함 — 완전 일치 요구는 인간에게 너무 가혹한가
- [ ] 자유 통신에 규정을 적용할 것인가 (적용이 현재안 — 안 하면 통신 구간이 안전지대가 된다)
- [ ] 인간이 2명/1명으로 줄었을 때의 보정 장치 필요 여부 (남은 인간에게 스푸핑 1회 추가?)
- [ ] 리더 교체를 실제 규칙으로 넣을지, 연출로만 둘지
- [ ] 이미지 생성 API 텍스처 옵션 — 외부 서비스 지연·비용·스타일 검토 후 (기본은 미채택)
- [ ] 3D 아바타·구역 에셋 출처 (원작 자산 재활용 가능 범위 확인 — 프리셋이 필요한 건 이 둘뿐)
- [ ] TTS 최종 선택 — 리더 목소리는 "안내 방송" 톤 (한국어 품질 비교 후)
- [ ] 인간 3명이 다 모이지 않을 때 — 대기? AI가 인간 자리를 연기해 채우기?
- [ ] 재접속·이탈 처리 — 인간이 나가면 그 노드는 어떻게 되는가 (정체 노출 없이 처리해야 함)
- [ ] 진실의 조각 텍스트 집필 (판마다 1조각 × N판)
