# 🕳️ Who is human — 인간인 척

> **2026년,** 대한민국 정부가 AI 식별 표지 부착을 의무화한다. 그해, 한 AI 설계자의 실수로
> **표지가 붙지 않은 휴머노이드 개체들**이 출고되어 인간 사회로 흘러든다. 말투도 표정도
> 인간과 완벽히 같다 — 다른 것은 **몸이 물리법칙에 반응하는 방식**뿐이다.
>
> 정부는 의심 인물들을 비밀 시설로 소집한다. 시설은 주기적으로 중력 · 마찰 · 빛과 색이 매번
> 달라지는 물리 테스트를 열어 정체를 가려낸다. 시스템은 아무도 판정하지 않는다 — **기록을
> 보여줄 뿐, 의심도를 움직이는 것은 사람들의 말과 실시간 지목뿐이다.** 의심도가 **100%**에
> 닿는 사람은 그 자리에서 즉시 격리된다 — 라운드가 끝나길 기다리지 않는다.
>
> 방 안에는 **표지 없는 AI 1개체**와, 표지를 붙이지 않은 걸 들켜서는 안 되는 **내부 AI
> 설계자(0~2명, 아무도 수를 모른다)**가 인간들 틈에 섞여 있다. AI 설계자는 각자 게임당
> 1회, 누군가의 기록을 조작할 수 있고 **시작부터 AI의 정체를 정확히 알고 있다.** 대신
> AI는 설계자가 누군지 모른다 — 아무도 서로의 정체를 모르는 이 게임에서 **유일하게
> 뚫려 있는 한 방향**이다.

- **플랫폼**: 웹 (Vite + React + Redux + Three.js / R3F) — `Who-is-human` 코드베이스를 이식해 시작한다
- **인원**: 인원은 고정이 아니라 **가변**이다(§1.1) — **사람 3~8명**(실제 플레이어), **AI
  항상 1명**(LLM, 고정 추가 좌석), **AI 설계자 0~2명**(사람 수에 따른 상한 안에서 랜덤,
  실제 플레이어 중에서 배정). 방을 만들 때 정하는 값은 없다 — 사람이 모이는 대로 시작
  조건만 채우면 된다
- **한 판**: **라운드로 나뉘지 않는다** — 물리 테스트가 주기적으로 열리고, 의심도 100%로
  격리된 인원이 **그 판 총원의 절반**(§1.3)에 도달하면 종료. 목표 플레이 시간 **7~9분**
  (하드캡 제안 10분)
- **계기**: 2026 NHN GAME X AI 해커톤 출품. 기존 `Who-is-human`(기계인 척)과 별도 레포(`NHNAI`)에서
  진행하며, 3D 멀티플레이 인프라와 Cloudflare Workers 아키텍처를 그대로 물려받는다

---

## 0. 이전 기획과 무엇이 다른가

| | 기계인 척 (이전) | **인간인 척 (새 기획)** |
|---|---|---|
| 구성 | AI 5 + 인간 3 (8석, 고정) | **AI 1(고정) + AI 설계자 0~2(가변) + 사람 3~8(가변)** |
| 찾는 대상 | 섞여든 **인간 3명** | 섞여든 **AI 1개체** (+ 있을 수도 있는 숨은 조력자 AI 설계자) |
| 검사 방식 | 리더 AI가 **매 라운드 새 게임을 발명**(무한 조합) | **고정된 물리 테스트 3종**, 파라미터만 테스트마다 변경 |
| 판정 주체 | 서버 결정론 채점 → 3등급 판독 → 리더 발표 + 투표 | **시스템은 판정하지 않는다** — 원자료(편차) 공개 → 순수 토론 + 실시간 지목(투표 없음, 의심도 100%가 곧 판정) |
| 능동적 대립자 | 없음 (AI는 숨는 쪽, 리더는 중립 진행자) | **AI 설계자가 능동적 스파이**(있을 경우) — 기록 조작 권한 |
| 인원 스케일링 | 8석 고정 | **가변 인원(3~8명) — 사람 수에 비례해 역할 상한이 정해진다** |
| LLM 호출 | 7개 에이전트 (리더 1 + 일반 5 + 분석가 1) | **에이전트 2종**(AI 참가자 1 + 관리 AI 1) |
| 물리 판정 | 리듬 타임스탬프 트릭(넷코드 불필요) | **일부 실물리 시뮬레이션 필요**(§5.1, 신규 리스크) |

이전 기획의 핵심 자산 — **"정체를 드러내지 않고 몸으로 겨루게 하는" 3D 멀티플레이 인프라**
(`worker/`, `src/world/`, 방 = DO 패턴)는 그대로 재사용한다. 검사의 발명 주체(리더 AI → 고정
미니게임)와 판정 주체(서버 채점 → 순수 토론)가 반대 방향으로 바뀐다.

---

## 1. 게임 규칙

### 1.1 역할과 인원 스케일링

방을 만들 때 정하는 값은 없다 — **사람이 모이는 대로** 시작 조건을 채우면 방장이 시작할
수 있다. 방을 만들 때 고르는 값은 이름뿐이다.

**실제 플레이어(인간) 총원은 3~8명이다.** 그 안에서 **AI 설계자 역할이 0~2명에게 비밀리에
배정**된다 — 설계자도 "실제 플레이어 3~8명" 안에 포함된 사람이지, 그 위에 따로 더해지는
인원이 아니다("사람(일반) = 나머지 전부, AI 설계자 = 그중 0~2명"인 구조).

| 역할 | 수 | 공개 여부 | 아는 것 |
|---|---|---|---|
| 🔴 **AI** | **항상 1명**(고정, 실제 플레이어 3~8명과 별개로 합류하는 LLM 좌석) | 존재는 공개, 좌석은 비공개 | 자기 자신이 AI라는 것뿐, AI 설계자가 누군지 모른다 |
| 🟡 **AI 설계자** | 실제 플레이어 3~8명 중 **0~2명**(사람 수에 따른 상한 안에서 랜덤, 아래 표) | 존재 여부 · 수 모두 비공개 | 자기 자신이 AI 설계자라는 것, AI의 정체를 시작부터 정확히 안다 |
| 🟢 **사람(일반)** | 실제 플레이어 중 **AI 설계자가 아닌 나머지 전부** | 수는 실시간 공개(전체 3~8명 기준) | 자기 자신이 사람이라는 것뿐 |

**AI 설계자 수의 상한은 실제 플레이어 수로 정해진다:**

| 실제 플레이어 수 | AI 설계자 상한 |
|---|---|
| 3명 | 0명 |
| 4~5명 | 1명 |
| 6~8명 | 2명 |

그 상한 안에서 0부터 균등 랜덤으로 뽑힌다 — **인원을 알아도 실제 설계자 수는 알 수 없다.**
**0명인 판도 정상이다**(그 경우 AI는 완전히 혼자다 — 순수하게 "사람들 틈의 AI 1개체 찾기"가
된다). 설계자가 둘이면 **서로도 서로를 모른다** — 각자 독립적으로 AI를 알고, 각자 게임당
1회 조작권을 갖는다(§1.4 P7은 "설계자 1인당"으로 적용).

- 게임을 시작하는 순간 **좌석이 다시 섞이고, AI와 AI 설계자(들)도 그 순열에 포함된다**
  (이전 기획 I7 계승) — 입장
  순서 · 좌석 번호로는 아무것도 알 수 없다.
- 사람 입장에서는 AI를 찾는 것과 (있을 수도, 없을 수도 있는) AI 설계자를 찾는 것이 뒤섞인다.

### 1.2 게임 루프 — 라운드 없이 연속으로 진행된다

```
입장 → 브리핑 → 물리 테스트가 주기적으로 열린다
              (테스트 종료 → 전체 화면 결과 모달(5~8초) → 모달 닫힘 → 토론 · 실시간 지목,
               이 사이클이 정해진 간격으로 반복된다)
           → 누군가의 의심도가 100%에 닿으면 그 즉시 격리 + 정체 공개
           → 격리 인원이 그 판 총원의 절반(§1.3)에 도달하면 종료
```

라운드라는 경계가 없다 — **테스트는 계속 열리고, 의심도는 계속 흐른다.** 유일한 이벤트
경계는 **격리 순간**이다.

| 요소 | 제안 값 | 비고 |
|---|---|---|
| 테스트 트리거 간격 | 60~90초마다 새 물리 테스트 개시 | 관리 AI가 3종 중 하나를 선택(§4.1) |
| 테스트 진행 시간 | 30~45초 | 전원 동시 참여, 종료 시 일괄 공개(이전 기획 I2 계승) |
| 결과 모달 | 5~8초, **전체 화면을 덮는 결과 창** | 테스트 종료 직후 전원 입력이 잠기고 전원이 동시에 같은 결과를 본다(§3) — 임팩트를 위한 의도적 정지 |
| 자유 토론 · 실시간 지목 | 모달이 닫힌 뒤 상시 | 결과 모달이 닫히면 재개 — 다음 테스트가 열려도 채팅 · 지목은 끊기지 않는다 |

**의심도는 시간이 지난다고 저절로 내려가지 않는다 — 오직 대화로만 풀린다.** (확정)
오르는 길과 내리는 길이 대칭이다: 상승도 발언, 하강도 발언이다.

| 트리거 | 의심도 | 비고 |
|---|---|---|
| 발언에서 특정 인물을 지목 | **+8** | |
| 그 지목에 동조하는 발언 | **+5** | |
| **몰이** — 동시에 2인 이상이 같은 대상을 지목 중 | 지목마다 **+2** 가산(최대 +6까지) | |
| 지목한 사람이 스스로 지목을 철회 | **−8** (건 만큼 되돌림) | 이전 기획의 "몰이가 풀림(해명이 먹혔다)"과 같은 자리 |
| 관리 AI 주장 판정에서 **해명이 로그와 일치**로 판정 | **−10** | §4.2 주장 판정 결과가 곧 이 트리거다 |
| 관리 AI 주장 판정에서 **해명이 로그와 불일치**(거짓 해명)로 판정 | **+10** | 거짓 해명은 안 하느니만 못하게 만드는 역-유인 |

(제안값 — 플레이테스트로 조정, §10)

**100%에 닿으면 즉시 격리**된다(이전 기획의 "의심도 100% = 폐기" 원칙 계승). 핵심 차이는
상승 요인이다 — 이전 기획은 검사 위반이 주 요인이었지만, 이 기획은 **오직 발언 · 지목,
그리고 그 발언에 대한 관리 AI의 판정만**(P1) 의심도를 움직인다.

### 1.3 승패

| 결과 | 조건 |
|---|---|
| 🔴 **AI 승리** | 격리되지 않고 게임 종료 시점까지 생존 |
| 🟡 **AI 설계자 승리** | **AI와 본인 둘 다** 격리되지 않고 게임 종료 시점까지 생존 |
| 🟢 **사람 승리** | AI가 격리됨 |

**게임은 격리 인원이 그 판 총원(실제 플레이어 3~8명 + AI 1)의 절반에 도달하면 종료된다.**
AI 설계자는 실제 플레이어 안에 이미 포함돼 있으므로 따로 더하지 않는다(§1.1). (확정)
인원이 가변이라 총원도 매판 다르다 — 예를 들어 실제 플레이어 3명이면 총원 4명(3명 + AI 1)
이라 **2명**이 격리되면 종료되고, 실제 플레이어 8명(그중 최대 2명이 설계자)이면 총원 9명
(8명 + AI 1)이라 **4~5명**이 격리되면 종료된다. 소수점 반올림 규칙(내림 vs 올림)은 §10
미정 — 짝수 총원에서는 무관하다.

절반을 기준으로 잡은 이유는 둘이다 — ① 사람 쪽에 "완전히 확신이 설 때까지 기다리기"를
허용하지 않으면서도 무근거 난사를 벌하는 균형점이고, ② 인원이 늘어날수록 판별에 쓸 수 있는
시간(테스트 트리거 횟수)도 늘어나므로, 목표 수를 총원에 비례시켜야 작은 판과 큰 판의
난이도 곡선이 비슷하게 유지된다.

AI 설계자가 격리되는 순간 그 설계자 개인의 승리 조건은 그 자리에서 무산된다(패배 확정) —
다만 AI 대 사람의 대결 자체는 계속된다(설계자가 둘이면 한쪽만 잡혀도 다른 한쪽은 계속
플레이한다). 사람이 서로를 잘못 지목해 격리해도 마찬가지로 게임은 계속된다 — 목표 인원이
격리됐는데 그 안에 AI가 없으면 AI(와 그때까지 생존한 AI 설계자 각각) 승리로 종료된다.

### 1.4 판별 원칙 — 이 게임의 헌법

*(이전 기획의 "불변 규칙 I1~I10"과 같은 위상이다. 클라이언트로 내려가는 필드를 추가할 때마다
"이 값이 정체를 특정하게 하는가"를 먼저 묻는다.)*

| # | 원칙 | 어기면 생기는 일 |
|---|---|---|
| **P1** | **시스템은 어떤 스코어도 의심도에 자동 반영하지 않는다.** 의심도는 실시간 지목 · 동조 발언으로만 움직인다(§1.2) | 채점 게임이 되어 대화가 사라진다 |
| **P2** | **절대 임계값을 쓰지 않는다.** 모든 판정 근거는 **직전 동일 테스트의 무리 평균 대비 편차**로만 계산해 노출한다 | "AI는 X초 안에 반응한다" 같은 고정 규칙이 뚫린다 |
| **P3** | **전환 구간(조건 변경 직후 N초)을 별도 태깅해 기록에 노출**한다 | 진짜 신호(적응 곡선)가 총점에 묻힌다 |
| **P4** | **오차의 방향(부호 있는 값)을 절대 오차와 별도로 기록·노출**한다 | 판별의 두 번째 축이 사라진다 |
| **P5** | **관리 AI에게도 정체표를 주지 않는다**(이전 기획 I1 계승). 해설·판정은 공개된 원자료만 근거로 한다 | 관리 AI가 편파적으로 해설해 사실상 정답을 유출한다 |
| **P6** | **주장 판정 LLM 호출은 실시간 물리 루프 밖(토론 단계)에서만** 실행한다 | 1~3초 응답 지연이 물리 판정 프레임에 끼어든다 |
| **P7** | **AI 설계자의 기록 조작은 설계자 1인당 판당 1회 · 대상 1명.** 설계자가 둘이면 각자 독립적으로 1회씩 쓴다. 조작 여부는 대상 본인을 포함해 그 설계자 외 아무도 모른다(다른 설계자도 모른다) | 공개되면 즉시 지목당하거나, 반대로 무제한 남용된다 |
| **P8** | **물리 테스트의 조건값(중력 배율 · 마찰 계수 등)은 클라이언트에 원값을 절대 내려주지 않는다** | 콘솔 조회로 정답이 새고, AI가 "일부러 틀리기"를 정확히 계산해버린다 |
| **P9** | **AI 참가자의 물리 조작 입력은 LLM이 실시간으로 만들지 않는다.** 서버가 인간 반응 분포에서 파라미터를 뽑아 시뮬레이션한다(§4.3, 이전 기획 I9 계승) | LLM 응답 지연 · 패턴으로 AI 좌석이 그대로 드러난다 |
| **P10** | **AI 발화(채팅)는 사람과 동일한 스트림 · 지터**로 나간다 | 타이핑 인디케이터 · 지연 패턴이 AI를 특정한다 |

---

## 2. 물리 테스트 명세

*(이전 기획은 리더가 매 라운드 게임 자체를 발명했다. 이 기획은 물리 미니게임을 **3종으로
고정**하고, 관리 AI는 그 안에서 파라미터와 순서만 정한다(§4.1) — 실시간 물리 시뮬레이션에는
서버 결정론 검사기 같은 자유도를 줄 수 없기 때문이다.)*

**3종으로 확정 — 확장하지 않는다.** (제안됐던 굴러오는 구체 · 낙하 예측 · 컨베이어 벨트 등의
추가 종목은 범위에서 제외한다. 구현 리스크를 줄이고 §9 우선순위 1~3번에 집중한다.)

### 2.1 낙하 생존 (중력) — ★ 메인, 최우선 구현

| | |
|---|---|
| 조작 | WASD 이동, Space 점프 |
| 물리 변수 | 중력 배율(100→60→140%), 공기 저항, 반발 계수 — **원값 비공개**(P8) |
| 기록 | 생존 시간 · 충돌 횟수 · **전환 직후 3초 오차**(P3) · 적응까지 걸린 시도 수 |
| 판정 방식 | **서버 권위 물리 시뮬레이션 필요** — 이전 기획의 "타임스탬프 트릭"이 안 통하는 유일한 종목(§5.1) |

### 2.2 정지선 (마찰 · 관성)

| | |
|---|---|
| 조작 | W 달리기, S 브레이크 |
| 물리 변수 | 마찰 계수(빙판→콘크리트→고무) |
| 기록 | 정지선 오차 거리 · **오차 방향**(P4) · 브레이크 시점 |
| 판정 방식 | **분석 공식으로 충분** — 속도 · 마찰 계수 · 브레이크 시각을 운동방정식에 대입해 기대 정지 거리를 서버가 계산하고 실제 정지 위치와 비교한다. 실시간 물리 시뮬레이션 불필요 → 구현 난이도 최저 |

### 2.3 색 사냥 (빛과 색) — 시연용

| | |
|---|---|
| 조작 | WASD 이동, E 줍기 |
| 물리 변수 | 조명 파장 차단(적 · 녹 · 청) — 셰이더 1장 |
| 기록 | 정답률 · 색 차단 후의 행동 · 헤맨 시간 |
| 판정 방식 | 이벤트 기반 — 주운 오브젝트 id와 "이번 테스트의 정답 색 목록"을 서버가 대조. 물리 시뮬레이션 없음 |

각 테스트의 **첫 실행은 기준 조건**으로 진행한다 — 평상시 기록이 있어야 조건이 바뀌었을 때
편차(P2)가 보인다.

---

## 3. 기록 공개

- 테스트가 끝나면 **전체 화면을 덮는 결과 모달**이 5~8초간 뜬다 — 전원의 입력이 잠기고,
  전원이 같은 순간에 같은 결과를 본다. 흩어져 있던 시선을 한곳에 모으는 의도적 정지다.
- 모달 안에는 무리 평균과 각자의 편차를 **원자료 그대로** 보여준다(이전 기획은 3등급으로
  뭉갰지만, 이 게임은 **해석의 여지를 일부러 남긴다** — 애매한 숫자가 토론의 재료다).
- 전환 구간 오차(P3)와 오차 방향(P4)은 모달 안에서 별도 필드로 강조 표시한다.
- 모달이 닫히면 자유 이동 · 토론 · 지목이 재개된다. **결과는 사라지지 않는다** — 화면 한쪽에
  요약 패널(HUD)로 남아, 다음 테스트가 갱신할 때까지 다시 열어볼 수 있다.
- **AI 설계자가 조작한 기록은 조작된 값으로 그대로 공개된다** — 모달에도, HUD에도 조작본만
  뜬다. 서버 로그에만 원본이 남는다(P7).

```
예시 — 낙하 생존, 중력 전환 직후 오차
무리 평균 1.8m
SUBJECT 01   1.9m
SUBJECT 02   0.2m  ← 편차
SUBJECT 03   2.3m
```

---

## 4. 관리 AI

### 4.1 역할

| 역할 | 내용 |
|---|---|
| 테스트 설계 | 다음에 열 물리 테스트를, 어떤 파라미터로 낼지 결정(3종 중 선택 + 강도 조절, 60~90초 간격) |
| 기록 해설 | 편차를 짚어 방송 — 토론에 불을 붙인다 |
| 주장 판정 | "저 사람은 중력 바뀐 걸 늦게 알아챘다" 같은 자유 서술을 실제 로그와 대조 |

### 4.2 왜 주장 판정이 LLM이어야 하는가

표현이 달라도 같은 주장이면 같은 판정을 내려야 한다 — 문자열 매칭으로는 안 된다. 동시에
**관리 AI에게도 정답표를 주지 않는다**(P5) — 판정은 오직 공개된 원자료(편차 · 오차 방향 ·
타임스탬프)와 발화 로그만 근거로 한다.

### 4.3 AI 참가자 — 텍스트는 LLM, 물리는 서버 시뮬레이션

AI 참가자는 두 층으로 나뉜다.

- **발화(채팅 · 주장 · 투표)**: 진짜 LLM 호출. 페르소나를 갖고 토론에 참여하고, 자기 기록을
  변호하거나 남을 지목한다.
- **물리 테스트 입력**: LLM이 실시간으로 WASD를 누르지 않는다(P9). 테스트마다 서버가 **행동
  프로파일**(반응 지연 평균 · 표준편차, 전환 구간 적응 곡선의 기울기, 오차 방향의 일관성)을
  뽑아 입력을 시뮬레이션한다. AI 에이전트는 각 테스트 시작 전 **"이번엔 얼마나 티 나게
  반응할지"**를 한 번의 LLM 호출로 전략적으로 정하고, 실행은 서버가 한다 — 이전 기획 I9와
  같은 이유다(실시간 프레임 단위 제어를 LLM에 맡기면 지연 · 불연속으로 오히려 더 티가 난다).

### 4.4 모델 · API 사용 규격 (제안)

라운드가 없어 호출 수는 **게임 길이(테스트 트리거 수)에 비례**한다. 8분짜리 판 · 75초
간격이면 테스트가 약 6~7회 열린다고 가정한 추정치다.

| 에이전트 | 모델 | 호출 빈도(판당, 추정) | 역할 |
|---|---|---|---|
| 관리 AI | `claude-opus-5` — 설계는 `effort: high` + `thinking`, 해설 · 판정은 `effort: low` | 테스트마다 2회(설계 · 해설) × 6~7회 + 주장 판정(지목 발생 시마다, ≈4~6회) ≈ **16~20회** | 테스트 설계, 기록 해설, 주장 판정 |
| AI 참가자 | `claude-sonnet-5` | 테스트마다 전략 갱신 1회 × 6~7회 + 토론 발화 수시(≈6~8회) ≈ **12~15회** | 물리 프로파일 전략, 채팅, 지목 대응 |

- 관리 AI의 테스트 설계 · 주장 판정은 **구조화 출력**으로 받는다(자유 텍스트 파싱 금지).
- 프롬프트 캐싱: 세계관 · 규칙 · 누적 기록은 프리픽스 고정 + `cache_control: { type: "ephemeral" }`.
- API 키는 브라우저에 절대 넣지 않는다 — 모든 호출은 Worker/DO 안에서(이전 기획과 동일).
- **LLM 호출은 실시간 물리 루프 밖에서만**(P6) — 테스트 진행 중엔 호출하지 않고, 종료 후
  기록 공개 · 토론 단계에서만 부른다.

---

## 5. 기술 아키텍처

```
[브라우저]                        [Cloudflare]                      [Anthropic]
Vite + React + Redux        Worker (정적 서빙 + API)            Messages API
R3F/three.js — 3D 시설    ←WS→  Durable Object = 방 1개           - 관리 AI  : opus-5 (thinking)
페이즈 UI / 기록 전광판          - 테스트 스케줄러 · 의심도 상태머신   - AI 참가자: sonnet-5
물리 미니게임 3종 렌더           - 물리 판정(2.1 실시간 / 2.2·2.3 이벤트)
                                - AI 설계자 조작 권한 처리(P7)
                                - 정체표 보관(절대 미송출)
```

- **`worker/` 는 이미 있다**(`Who-is-human`의 `RoomDO` — 입장 · 이동 · 채팅 중계). 여기에 **테스트
  스케줄러 · 의심도 상태머신 · 물리 판정기 · AI 설계자 조작 API · 정체표**를 얹는다.
- 방 하나 = DO 인스턴스 하나(이전 기획과 동일한 근거로 동일한 선택).

### 5.1 신규 리스크 — 낙하 생존의 서버 권위 물리

이전 기획은 리듬 게임류라 **타임스탬프만 비교하면 판정이 끝났다**(넷코드 불필요). 낙하 생존은
다르다 — 중력이 바뀐 공간에서 캐릭터가 실제로 낙하 · 충돌해야 하므로:

- 서버가 **매 틱 결정론적 물리 스텝**(중력 · 충돌만 있는 단순 커스텀 시뮬 — three.js 물리
  엔진 풀스택은 과함)을 돌리고 클라이언트는 입력만 보낸다(서버 권위), 또는
- 클라이언트가 예측 실행하고 서버는 **주기적 스냅샷 대조**로 오차만 기록한다(더 가볍지만
  판정 정합성은 낮아진다).

**이 항목이 가장 먼저 프로토타입으로 검증돼야 한다** — GAME 2 · 3은 분석 공식/이벤트 기반이라
리스크가 낮다.

### 5.2 그대로 재사용

- 정체표 · 채점은 서버(DO)에서만, Workers는 동적 코드 실행이 막혀 있어야 한다는 원칙(이전 기획 계승)
- 배포 단위 하나, 시크릿은 `.dev.vars`에만, Claude는 배포 · 푸시 금지(이전 기획 계승)

---

## 6. 화면 · 폴더 매핑 (재사용 검토)

| feature | 이 게임에서 맡는 것 | 이식 방향 |
|---|---|---|
| `world/` | 3D 시설 공간, 가변 좌석(최대 9석 = 사람 8 + AI 1), 중앙 판별 장치 | `Who-is-human`의 구역 공간을 재활용, 가변 좌석 패턴(§1.1) 적용 |
| `arena/` (신규 또는 재활용) | 물리 테스트 3종 렌더 + 판정 | 낙하 생존이 가장 무거우니 별도 서브피처로 분리 검토 |
| `talk/` (이식) | 토론 페이즈 자유 채팅 | 규정 HUD 등 이전 기획 전용 UI는 제거 |
| `lobby/` | 방 만들기 / 3~8인 대기방(정원 선택 없음) | 정원을 미리 정하지 않는 패턴 적용 |
| `llm/` (신규) | 관제 화면 — 관리 AI 해설 · 판정 로그 | 이전 기획의 "리더 사고 로그" 개념 재사용 |
| `tts/` | 관리 AI 방송 음성 | 그대로 |

*(정확한 재사용 범위는 각 폴더 실제 코드 감사가 필요하다 — 여기서는 방향만 제시한다.)*

---

## 7. 연출

- **로우폴리 + 플랫 셰이딩**, 텍스처 최소화 — 색이 판별 요소(§2.3)라 텍스처가 색을 흐리면
  손해고, 라이트 베이킹 시간도 절약된다
- **같은 맵, 상수만 교체** — 에셋 하나로 테스트를 여러 번 반복
- 캐릭터는 1종 + 색상 변형으로 최대 9명(사람 8 + AI 1)까지 커버
- **랜드마크(중앙 판별 장치) 하나만 제대로** — 심사에 남는 건 그 한 컷

---

## 8. 비용 (추정)

관리 AI 16~20회(opus-5, 설계만 thinking) + AI 참가자 12~15회(sonnet-5) — 이전 기획(7개 에이전트,
판당 ≈70회 호출)보다 **에이전트 수가 3분의 1**이라 판당 LLM 호출도 크게 줄어든다.

**한 판 추정**: ≈ **$0.2~0.4**, 프롬프트 캐싱 적용 시 더 낮아진다. 해커톤 시연 20판이면 $4~8
수준 — 이전 기획($13~20/20판)보다 낮다.

---

## 9. 개발 우선순위

*(해커톤 일정이 아직 확정되지 않아, 이전 기획처럼 주차별 마일스톤 대신 어떤 순서로 잘라도
데모가 성립하는 **우선순위**로 관리한다.)*

| 순위 | 항목 | 비고 |
|---|---|---|
| 1 | **GAME 1 낙하 생존** | 판별 원리의 핵심이자 유일한 신규 리스크(§5.1) — 가장 먼저 프로토타입 |
| 2 | **기록 공개 UI** | 전체 화면 결과 모달(§3) + 편차 표시. 게임의 재미가 여기서 나옴 |
| 3 | **토론 · 실시간 지목/의심도 UI** | `Who-is-human` 채팅 · 지목(E) 인프라 재사용 |
| 4 | **GAME 2 정지선** | 구현 난이도 최저(분석 공식) |
| 5 | **관리 AI 프롬프트** | 기록 해설 + 주장 판정 |
| 6 | **AI 설계자 조작 권한** | P7 — 판당 1회, 서버 API 하나만 있으면 됨 |
| 7 | **GAME 3 색 사냥** | 시연 임팩트, 셰이더 1장 |

**주의**: 미니게임을 늘리는 것보다 **기록 UI와 토론 단계를 다듬는 것**이 우선이다 — 재미는
미니게임이 아니라 그 뒤의 의심에서 나온다.

**폴백**: 싱글 로직(관리 AI + AI 참가자 시뮬레이션)을 먼저 완성하고 멀티를 얹는다. 접속 실패
시 일부 참가자를 NPC로 대체하는 폴백을 준비해 시연 사고를 방지한다.

---

## 10. 미정 항목 (진행하며 결정)

- [x] ~~국가 표기~~ → **대한민국**으로 확정
- [x] ~~AI 설계자에게 AI 힌트를 줄지~~ → **시작부터 AI의 정체를 정확히 알려준다**로 확정(§1.1)
- [x] ~~라운드마다 몇 명을 격리하는지~~ → **라운드 자체를 없앤다.** 의심도 100% = 즉시 격리(§1.2)로 확정
- [x] ~~소집 방식~~ → **'소집'**으로 확정(납치는 AI를 찾을 동기가 사라짐)
- [x] ~~목표 격리 인원 수~~ → **그 판 총원(실제 플레이어 + AI 1)의 절반**으로 확정(§1.3, 고정
      인원 전제였던 이전 "3명" 답은 인원 스케일링 결정으로 대체)
- [x] ~~인원 구성이 고정인지 가변인지~~ → **가변** — 실제 플레이어 3~8명, AI 항상 1명, AI
      설계자는 그 3~8명 중 0~2명(사람 수 비례 상한, §1.1)으로 확정
- [x] ~~의심도가 시간이 지나면 자연 감소하는지~~ → **자연 감소 없음, 오직 대화(지목 철회 ·
      관리 AI의 해명 판정)로만 내려간다**로 확정(§1.2)
- [x] ~~물리 테스트를 3종 외에 확장할지~~ → **3종으로 확정, 확장하지 않는다**(§2)
- [ ] 총원이 홀수일 때 절반의 반올림 규칙 — 내림(느슨) vs 올림(엄격) (§1.3)
- [ ] 결과 모달의 정확한 노출 시간(제안 5~8초)과 스킵 가능 여부(전원 확인 시 조기 종료?) (§3)
- [ ] §1.2 의심도 상승 · 하강치(+8/+5/+2/−8/−10/+10)와 테스트 트리거 간격(60~90초)은
      플레이테스트로 조정
- [ ] §5.1 서버 권위 물리의 구체 방식 — 커스텀 결정론 스텝 vs 클라 예측 + 스냅샷 대조
- [ ] AI 참가자의 "일부러 틀리기" 확률 · 강도 분포 — 너무 완벽하면 금방 들통나고, 너무 티
      나면 재미가 없다
- [ ] 3D 아바타 · 시설 에셋 출처 — `Who-is-human` 자산 재활용 가능 범위
- [ ] TTS 최종 선택 — 관리 AI 목소리 톤
